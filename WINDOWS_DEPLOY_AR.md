# دليل نشر نسخة ويندوز — إنتاج وتغليف وتوقيع

> هذا الدليل يخصّ **بناء ويندوز وحزمه ونشره**. لهوية التطبيق المشتركة
> (المعرّف، الاسم، الأيقونات) راجع `PACKAGING_AR.md`، ولأثر التشفير على
> النسخ الاحتياطي والاستعادة راجع `ENCRYPTION_AR.md` — **اقرأه قبل النشر**:
> فَقْدُ مفتاح الجهاز يعني قاعدةً لا تُفتح، والمزامنة السحابية هي النسخة
> الاحتياطية الوحيدة الفعّالة.

---

## ١) متطلبات البناء

البناء لا يتمّ إلا على **ويندوز** (سلسلة C++ لمجلد `windows/runner/` تحتاج MSVC):

| الأداة | الغرض |
|---|---|
| Windows 10 (1903+) أو 11، x64 | منصّة الترجمة والتشغيل المستهدفة |
| Flutter 3.44.8 (Dart 3.12) | نفس الإصدار المثبَّت في CI (`FLUTTER_VERSION`) |
| Visual Studio 2022 + عبء «Desktop development with C++» | مترجم runner |
| حزمة `msix` (dev dependency، مثبّتة) | توليد MSIX |

> **لا يُبنى على لينكس.** بيئة التطوير اللينكسية تحلّل وتختبر المنطق، أما
> ترجمة سطح المكتب فمكانها جهاز ويندوز أو وظيفة `build-windows` في CI.

---

## ٢) أمر الإنتاج

من جذر المشروع على ويندوز:

```bat
flutter build windows --release
dart run msix:create
```

أو عبر السكربت الجاهز (يقرأ الأسرار من `tool\cloud.env` أو البيئة):

```bat
tool\build_windows.bat            :: موصول بالسحابة إن توفّر cloud.env
tool\build_windows.bat --local    :: محلي صريح بلا أسرار
```

```powershell
pwsh tool\build_windows.ps1 -Local
```

**حقن الأسرار (اختياري):** نسخة سطح المكتب تعمل بوضع محلي بلا سحابة، فالبناء
بلا مفاتيح مشروع. لتفعيل المزامنة احقن وقت الترجمة (كما في `PACKAGING_AR.md`):

```bat
flutter build windows --release ^
  --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=... ^
  --dart-define=R2_WORKER=...
```

السكربتان يفعلان ذلك تلقائياً حين يجدان القيم — بلا سرّ ثابت في المستودع
(حارس الأسرار في CI يمنع تسرّبها).

---

## ٣) أين يظهر الناتج

| الناتج | المسار |
|---|---|
| المجلد المحمول (EXE + DLLs + `data\`) | `build\windows\x64\runner\Release\` |
| ملف الحزمة | `build\windows\x64\runner\Release\*.msix` |

- **EXE المحمول:** انسخ مجلد `Release\` كاملاً إلى الجهاز الهدف وشغّل
  `dental_clinic_flutter.exe`. لا مثبِّت — يعمل من مكانه.
- **MSIX:** ملف تثبيت واحد؛ التوزيع أنظف والإزالة تنظّف نفسها (القسم ٥).

نافذة التطبيق تفتح بحجم 1360×860 ولا تُصغَّر تحت 1000×640 (حدّ فرضه
`windows/runner/`) كي لا ينكسر تخطيط سطح المكتب.

---

## ٤) التوقيع

MSIX **غير موقَّعة لا تُثبَّت** على ويندوز (إلا بتفعيل وضع المطوّر). خياران:

### أ) شهادة ذاتية التوقيع — للتجربة الداخلية فقط

```powershell
# أنشئ الشهادة (Subject يطابق publisher في msix_config: CN=Dental Clinic)
New-SelfSignedCertificate -Type Custom -Subject "CN=Dental Clinic" `
  -KeyUsage DigitalSignature -CertStoreLocation "Cert:\CurrentUser\My" `
  -TextExtension @("2.5.29.37={text}1.3.6.1.5.5.7.3.3")

# صدّرها .pfx بكلمة مرور، ثم مرّرها لأداة MSIX (كلمة المرور من البيئة لا نصاً):
dart run msix:create --certificate-path "dental.pfx" `
  --certificate-password "$env:MSIX_CERT_PASSWORD"
```

على **كل جهاز هدف** استورد الشهادة العامة إلى
`Local Machine → Trusted People` وإلا رُفض التثبيت. مناسبٌ لأجهزة العيادة
المعروفة، لا للتوزيع الواسع.

### ب) شهادة Authenticode حقيقية — للنشر

من مرجع موثوق (DigiCert/Sectigo…). موضوعها (Subject/CN) **يجب أن يطابق**
`publisher: CN=Dental Clinic` في `msix_config` بـ `pubspec.yaml` حرفاً بحرف،
وإلا رفض ويندوز الحزمة. حدّث القيمة هناك لتطابق شهادتك، ثم مرّر المسار
وكلمة المرور كما أعلاه (أو أضفهما `certificate_path`/`certificate_password`
داخل `msix_config`). بها تُثبَّت الحزمة بلا استيراد يدوي على الأجهزة.

> **لماذا publisher نائب في المستودع:** القيمة `CN=Dental Clinic` علامةٌ
> تُستبدل بموضوع الشهادة الفعلي — لا تُشحن شهادة ولا كلمة مرور في git.

---

## ٥) مكان البيانات وإلغاء التثبيت

**البيانات في `%LOCALAPPDATA%` لا `%APPDATA%` (Roaming).** السبب مشروحٌ في
`ENCRYPTION_AR.md` §٢: مجلد Roaming يُنسَخ إلى خادم الشبكة في بيئات النطاق —
أي نسخُ قاعدة مرضى مشفَّرة (بلا مفتاحها) خلف ظهر المالك، مع خطر تلف SQLite
على مسار يُزامَن. التطبيق ينقل مجلده من Roaming إلى Local **تلقائياً مرّة
واحدة** عند أول تشغيل بعد الترقية، ويترك `MOVED_TO.txt` يدلّ على الوجهة.

المسار الفعلي نحو:
`C:\Users\<اسم المستخدم>\AppData\Local\com.dental.clinic\` (قاعدة مشفَّرة،
خزنة الأشعة، الإعداد، السجلات).

### إلغاء التثبيت — فرقٌ جوهري بين الطريقتين

| الطريقة | ماذا يُحذف تلقائياً | ماذا يبقى |
|---|---|---|
| **MSIX** (إعدادات ويندوز ← التطبيقات ← إزالة) | حاوية التطبيق **وبياناتها في `%LOCALAPPDATA%`** — يزيلها النظام تلقائياً | لا شيء محلياً |
| **EXE محمول** (حذف مجلد `Release\`) | ملفات البرنامج فقط | **مجلد `%LOCALAPPDATA%\com.dental.clinic\` يبقى** — احذفه يدوياً |

> **لماذا الفرق:** MSIX تعزل بيانات التطبيق في حاوية يملكها النظام فيطويها
> مع الإزالة. أما EXE المحمول فيكتب مباشرةً في `%LOCALAPPDATA%` خارج أي
> حاوية، فحذفُ مجلد البرنامج لا يمسّها. **لإزالة كاملة لنسخة EXE احذف مجلد
> `%LOCALAPPDATA%\com.dental.clinic\` يدوياً.**

> **تذكير أمني (من `ENCRYPTION_AR.md`):** حذفُ بيانات التطبيق يمحو القاعدة
> المشفَّرة، والمفتاح في مخزن النظام (DPAPI) قد يذهب معها. **لا تُزل التطبيق
> قبل التأكد أن المزامنة السحابية استرجعت كل شيء على جهاز آخر** — وإلا فما
> بقي محلياً ذهب بلا رجعة.

---

## ٦) حدود ما جرى التحقق منه هنا

مُهيَّأ ومُراجَع على مستوى الملفات (تحليل نظيف، YAML صالح، مراجعة C++
بصرية)، لكن ما يلي **يتطلّب جهاز ويندوز حقيقياً** ولم يُشغَّل في بيئة لينكس:

- ترجمة `windows/runner/` فعلياً — تفرضها وظيفة `build-windows` في CI.
- سلوك حدّ الحجم الأدنى وتحجيم DPI على شاشات متعددة الكثافة.
- توليد MSIX والتوقيع والتثبيت والإزالة الذاتية للبيانات.
- سلوك DPAPI ونقل مجلد Roaming→Local (راجع `ENCRYPTION_AR.md` §٦).
