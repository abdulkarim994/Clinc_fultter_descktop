# دليل إعداد السحابة (Supabase + R2) لتطبيق العيادة — Flutter

> **قرار التفعيل (2026-07-27، بطلب صريح من المالك):** بدءاً من النسخة
> التجريبية **v12** يُبنى التطبيق موصولاً **بمشروع الإنتاج نفسه** الذي
> يستخدمه تطبيق Vue (Supabase + عامل R2 من `.env.production`) مع أعلام
> الإنتاج نفسها (`PHONE_IDENTITY=1`, `COLD_FETCH=1`, `SYNC_V2`) —
> فالتطبيقان يتشاركان الحساب والبيانات جنباً إلى جنب. **الاختبارات تبقى
> محلية بالكامل** (خادم مزيف) ولا تلمس الإنتاج أبداً.
>
> **تحذير أمني قائم:** المفتاح العلني anon داخل أي عميل (كما في تطبيق Vue
> نفسه) — وإن كان أرشيف المشروع الأصلي قد شورك مع أي طرف، **دوّر مفاتيح
> المشروع** من لوحة Supabase (‏Settings → API → Rotate keys) ثم أعد بناء
> النسخة بالقيم الجديدة. بقية هذا الدليل يشرح إنشاء مشروع منفصل عند الحاجة
> (بيئة تجارب معزولة).

---

## الجزء 1 — مشروع Supabase جديد (المزامنة والمصادقة)

### 1. إنشاء المشروع
1. من <https://supabase.com/dashboard> أنشئ مشروعاً جديداً (اختر أقرب منطقة).
2. من **Settings → API** خذ قيمتين فقط:
   - **Project URL** — مثل `https://abcd1234.supabase.co`
   - **anon public key** — المفتاح العلني (لا تستخدم `service_role` في التطبيق أبداً)

### 2. تفعيل مصادقة البريد
- **Authentication → Providers → Email**: فعّل Email/Password.
- أثناء التجربة يُنصح بإيقاف **Confirm email** (وإلا فسيلزم تأكيد كل بريد
  قبل الدخول — رسالة «البريد لم يتم تأكيده بعد»).

### 3. تشغيل هجرات المخطط (بالترتيب، مرة واحدة)
افتح **SQL Editor** وشغّل ملفات `supabase/migrations/` من مشروع Vue الأصلي
**بترتيب أرقامها** — أو استخدم Supabase CLI (`supabase db push`):

| الملف | ماذا ينشئ |
|---|---|
| `0001_sync_core.sql` | جدول `sync_rows` (المفتاح user/entity/id + payload jsonb + `_hlc` + `server_seq`) |
| `0002_rls.sql` | سياسات RLS — كل مستخدم يرى صفوفه فقط (`auth.uid()`) |
| `0003_functions.sql` | دوال المزامنة الأساسية |
| `0004_queue_system.sql` | نظام الطابور |
| `0005_applied_ops_retention.sql` | سجل العمليات المطبقة `applied_ops` (منع تكرار op_id) |
| `0006_partition_sync_rows.sql` | تقسيم الجدول (أداء) |
| `0007_drop_payload_gin_optional.sql` | اختياري |
| `0008_read_rpcs.sql` | دوال قراءة |
| `0009_archive_prune.sql` | أرشفة/تقليم |
| `0010_apply_changes_atomic.sql` | **RPC `apply_changes`** — تطبيق دفعة عمليات ذرّياً بقاعدة HLC آخر-كاتب-يكسب |
| `0011_pull_changes_gapfree.sql` | **RPC `pull_changes`** — سحب بنافذة txid آمنة الالتزام (بلا فجوات) |

> تطبيق Flutter لا يستدعي غير `apply_changes` و`pull_changes` — وهما
> نفس العقد الذي أثبتته اختبارات التقارب على الخادم المزيف بايتاً ببايت.

### 4. ربط التطبيق
داخل التطبيق: **الإعدادات → الاتصال السحابي (Supabase) → تحرير**، أدخل
URL والمفتاح العلني ثم «حفظ»، وسجّل الخروج والدخول بحساب حقيقي (سجّل
حساباً جديداً من شاشة الدخول). البيانات المدخلة تُحفظ في
`cloud_config.json` بجوار قاعدة البيانات **محلياً فقط** (لا تُزامن أبداً).

بديل للنشر المُدار: التمرير وقت البناء —

```bash
flutter build windows \
  --dart-define=SUPABASE_URL=https://abcd1234.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=eyJ... \
  --dart-define=R2_WORKER=https://xray.example.workers.dev
```

### 5. اختبار القبول السريع
1. جهازان (أو نسختا تشغيل بمجلدي بيانات مختلفين) بالحساب نفسه.
2. أضف مريضاً وسجلاً على الجهاز الأول → زر «مزامنة».
3. زر «مزامنة» على الثاني → يظهر المريض والسجل.
4. عدّل الإعدادات على الجهازين معاً ثم زامن — تُدمج القوائم بنيوياً
   (لا يفقد أي طرف إضافته).

---

## الجزء 2 — صور الأشعة السحابية (Cloudflare R2) — اختياري

بدون هذا الجزء يعمل كل شيء، وتبقى صور الأشعة **محلية على كل جهاز** مع
مزامنة *بياناتها الوصفية* فقط (صف `xrays` والمصغرة تسافر عبر المزامنة).

الرفع السحابي يمر عبر **عامل Cloudflare** (كما في الأصل تماماً) بعقد HTTP:

| النداء | الوظيفة |
|---|---|
| `POST {worker}/upload?key=…` + جسم البايتات + ترويسات `Authorization: Bearer <jwt>`، `Content-Type`، `X-Patient-Name`، `X-File-Name` | يرفع الصورة ويعيد JSON فيه المفتاح الفعلي `{"key": "…"}` (قد يسكّ العامل مفتاحه الخاص) |
| `GET {worker}/image/{key}` (+`?v=thumb`) | يجلب الصورة (أو مصغرة خادمية) |
| `HEAD {worker}/image/{key}` | وجود/حجم الكائن (للتحقق قبل التنظيف) |
| `DELETE {worker}/image/{key}` | يحذف الكائن (404/410 = زال أصلاً) |

على العامل **التحقق من JWT ســوبابيز** في كل نداء (رفض ما لا يحمل
`Authorization: Bearer` صالحاً موقّعاً بسر JWT الخاص بمشروعك الجديد).

> **ملاحظة صدق:** شيفرة العامل نفسها ليست ضمن أرشيف مشروع Vue المسلَّم
> (منشورة لدى صاحب المشروع على حساب Cloudflare). أعِد نشر عاملك على العقد
> أعلاه (خطوات النشر: إنشاء R2 bucket ثم `wrangler deploy` لعامل يقرأ/يكتب
> فيه ويتحقق من JWT). ثم ضع رابط العامل في «الاتصال السحابي → R2 Worker URL».

بعد الربط: كل صورة التُقطت أوفلاين تبقى في «بانتظار الرفع» وتُرفع تلقائياً
بعد أول مزامنة ناجحة (تحقق-قبل-التنظيف: لا تُحذف نسخة محلية قبل التأكد
من سلامة النسخة السحابية بمقارنة SHA-256).

---

## الجزء 3 — ما الذي يتزامن وما الذي يبقى محلياً؟

| يتزامن عبر Supabase | يبقى على الجهاز |
|---|---|
| المرضى والسجلات والديون والتركيبات والمواعيد والطابور | `cloud_config.json` (الأسرار) |
| إعدادات `app.config` (دمج بنيوي حقلاً بحقل) | `session.json` (جلسة الدخول) |
| صفوف `xrays` الوصفية + المصغرات | بايتات الصور الكاملة (حتى رفع R2) |
| | ملفات PDF المصدَّرة (`exports/`) |

## الجزء 4 — استكشاف الأخطاء

- **«pull_changes: not authenticated»** — الجلسة انتهت ولم يُتَح تحديثها:
  سجّل الخروج والدخول. التطبيق يحدّث الرمز استباقياً قبل انتهائه بخمس دقائق.
- **رفض 401 متكرر بعد استعادة نسخة قديمة** — دوّرت المفاتيح؟ حدّث
  `cloud_config.json` من شاشة الإعدادات.
- **لا شيء يصل للجهاز الآخر** — تأكد أن الحسابين نفس البريد (عزل RLS
  يمنع رؤية صفوف مستخدم آخر — بالتصميم).
