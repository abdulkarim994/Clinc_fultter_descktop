# م88 — تسجيل الدخول بـGoogle عبر Supabase Auth (OAuth رسمي، PKCE)

> **حالة التسليم: مكتملة ✅ ومتحقَّقٌ منها على جهاز المالك الحقيقي
> (2026‑08‑01):** مسار المتصفح (PKCE) نجح على أندرويد في v1.0.5، ثم
> **النافذة الأصلية** (حسابات الجهاز داخل التطبيق، بلا متصفح) نجحت في
> v1.0.6 بعد إنشاء عميل أندرويد في Google. الاختبارات **723/723** خضراء
> والمحلل نظيف.

---

## ١) الخادم — Supabase (مطبَّق حيّاً ومتحقَّق منه)

### جدول `profiles` (هجرة `0030`)
- `profiles(id, email, full_name, avatar_url, created_at, updated_at)`
  بمفتاح `id` = `auth.users.id` — هويةٌ واحدة ⇒ صفٌّ واحد، **لا تكرار عند
  ربط الحساب**.
- تريغر `SECURITY DEFINER` على إنشاء/تحديث المستخدم يملأ الملف تلقائياً من
  بيانات المزوّد (‏Google: `full_name`/`avatar_url`، وغيره: `name`/`picture`).
  **مُثبَت حيّاً** بمستخدم تجريبي ثم نُظّف.
- RLS بنمط `sync_rows` حرفياً: `id = (select auth.uid())` قراءةً وتحديثاً
  وإدراجاً. **لا مساس بأي سياسة قائمة.** الحسابان القائمان مُلئا backfill.

### مزوّد Google (2026‑08‑01، عبر Management API، بتحقّق قراءةٍ مستقلّة)
- `external_google_enabled = true` والمعرّف/السرّ في الخادم **فقط** —
  لا سرّ في أي ملف داخل المشروع.
- `uri_allow_list`: ‏`dentaldk://login-callback` (أندرويد) +
  `http://localhost:43823` و`http://127.0.0.1:43823` (ويندوز).
- `mailer_autoconfirm = true` ⇒ **الربط التلقائي بنفس البريد يعمل**: من
  سجّل سابقاً بالبريد/كلمة المرور ثم دخل بـGoogle بنفس البريد تُضاف هويةُ
  Google إلى **نفس الحساب** — نفس `auth.uid` فتبقى كل بيانات عيادته كما هي
  (ولا مسحَ تبديل حساب، لأن المعرّف لم يتغيّر).

---

## ٢) التطبيق — الحزمة الرسمية `supabase_flutter 2.16.0`

### ما وُصل (وفق شروط م88 الحرفية)
| الشرط | التنفيذ |
|---|---|
| OAuth رسمي، لا مخصص | كل التدفّق من `supabase_flutter`: توليد PKCE، رابط `/authorize`، تبادل الرمز، بثّ `signedIn` |
| **PKCE لا Implicit** | `AuthFlowType.pkce` **نصّاً** في التهيئة، لا اتكالاً على الافتراض |
| **لا رموز بنصٍّ واضح** | مخزنا الحزمة (جلسة `sb-…-auth-token` + مُتحقِّق PKCE) حُوّلا لمخزن أسرار النظام (Keystore/DPAPI) عبر نقطتي التمديد الرسميتين `LocalStorage`/`GotrueAsyncStorage` — **وجلسةُ كلمة المرور أيضاً**: انتقلت من `session.json` النصّي إلى المخزن الآمن، مع **ترحيلٍ تلقائي** عند أول إقلاع (يبقى المستخدم داخلاً، ويُحذف الملف النصّي بعد نجاح النقل حصراً) |
| تجديد الجلسة وانتهاء الرمز | جلسة Google: تجديد الحزمة التلقائي + تجديدٌ عند قرب الانتهاء في مسار الرموز + مسار 401 (`forceRefresh`). جلسة كلمة المرور: المحرّك القائم كما هو |
| الخروج | يمرّ بالجسر: الحزمة تنظّف محلياً **قبل** نداء الإبطال — فيعمل أوفلاين |
| Offline-First محفوظ | مُتحقَّق من مصدر الحزمة: `setInitialSession` يستعيد دون شبكة ولو انتهى الرمز، وفشل التجديد الشبكي **لا يمسح** الجلسة. وفشل أي خطوة سحابية عند الإقلاع = تدهورٌ رشيق مسجَّل، لا شاشة سوداء |
| عدم المساس بالمزامنة | لم يتغيّر منطقها؛ فقط `validAccessToken/forceRefresh` صارا يقدّمان رمز جلسة Google حين تكون هي القائمة (نفس العقد) |
| التوجيه بعد الدخول | جديد ⇒ شاشة اسم المركز إجبارياً، عائد ⇒ ترحيبٌ باسمه — عبر البوابة القائمة (مختبَر بواجهتين) |

### قاعدة «محرّك جلسة واحد»
رمزُ تحديث GoTrue يُستهلك بالاستعمال؛ محرّكان على جلسة واحدة = سباقٌ
يقتلها. لذلك: جلسة Google يديرها SDK وحده، وجلسة كلمة المرور يديرها
المحرّك القائم وحده، **ودخول أيٍّ منهما يُسقط جلسة الآخر** (مختبَر).

### مسارا العودة
- **أندرويد** — deep-link ‏`dentaldk://login-callback`
  (متصفح خارجي؛ Google ترفض WebView). ‏`launchMode` رُفع إلى `singleTask`
  كي تصل العودة إلى **النسخة القائمة** من النشاط لا إلى محرّكٍ ثانٍ يفتح
  القاعدة المشفَّرة بالتوازي. ‏minSdk ارتفع 23→24 (شرط `app_links`؛ أثره
  العملي صفر).
- **ويندوز/لينكس** — خادم استماع محلي على المنفذ الثابت **43823** يستقبل
  `?code=` ثم `exchangeCodeForSession` (المنفذ مطابقٌ حرفياً لقائمة السماح).

---

## ٣) الاختبارات — 719/719 ✅ (‏702 سابقة + 17 جديدة، صفر انحدار)

| مجموعة م88/ب الجديدة | يغطي |
|---|---|
| مخزن الجلسة (6) | التوافق الملفّي التاريخي، **الترحيل الآمن** (نقلٌ ناجح/نسخة آمنة تفوز/ملفٌ تالف لا يُحذف/عطل قراءةٍ لا ينهار)، فورية القراءة ولحاق الكتابة |
| موزّع الجلسات (4) | جلسة Google ⇒ استعادةٌ ورموز من الجسر؛ لا جلسة ⇒ المسار القائم حرفياً؛ دخول كلمة المرور يُسقط جلسة Google؛ الخروج ينظّف الاثنين |
| مستقبِل loopback (4) | استقبال `?code=` بصفحة نجاح عربية (طلبات HTTP حقيقية)، تجاهل favicon، ترجمة `access_denied`، المهلة |
| مخازن الحزمة (3) | مطابقة مفتاح الجلسة لاشتقاق الحزمة، دورة `LocalStorage` كاملة، عزل بادئة مُتحقِّق PKCE |

مع اختبارات م88/أ الست (زر Google، جديد⇒إعداد، عائد⇒ترحيب باسمه، إلغاء،
خروج). `flutter analyze`: **0 أخطاء، 0 تحذيرات**.

### ما لا يُختبر في الصندوق — تُحقِّق منه المالك على جهازه (2026‑08‑01) ✅
- **v1.0.5**: دورة المتصفح كاملةً على أندرويد — فتح Google، اختيار حساب،
  عودة `dentaldk://login-callback`، دخولٌ ناجح. ✅
- **v1.0.6**: **النافذة الأصلية** (google_sign_in + `signInWithIdToken`) —
  حسابات الجهاز داخل التطبيق بلا متصفح، بعد إنشاء عميل أندرويد
  (`com.dental.clinic.debug` + بصمة مفتاح التطوير). ✅
- بندان مؤجَّلان بوعي: تجربة loopback **ويندوز** على جهاز ويندوز فعلي،
  وخطوة **PUBLISH APP** إن ظهر `access_denied` لحسابات ليست حساب المالك.

---

## ٦) م88/ج — ملاحظات المالك بعد أول تجربة حية (نُفِّذت وتُحقِّق منها)

| الملاحظة | التنفيذ |
|---|---|
| «بدل الرابط، اسم التطبيق» | النافذة الأصلية تعرض اسم التطبيق لا نطاق الخادم. (نطاق الخادم في **صفحة المتصفح** الاحتياطية لا يُستبدل إلا بـSupabase Custom Domain — مدفوع، مؤجَّل لوقت النشر) |
| «نافذة حسابات الهاتف مثل ChatGPT بلا متصفح» | google_sign_in v7 ‏(`serverClientId` = عميل الويب) ⇒ ‏idToken ⇒ ‏`signInWithIdToken` الرسمي. الإلغاء يُحترم؛ وتعذُّر المسار يسقط رشيقاً لمسار المتصفح — زرٌّ يعمل دائماً |
| «تسجيل خروج صغير في الإعداد الإجباري» | رابط `gate-logout` بخطٍّ رفيع أسفل «بدء تجهيز الحساب»: يخرج لشاشة الدخول، والعودةُ بنفس الحساب تعيده للإعداد الإجباري نفسه (مختبَر آلياً) |

ملفات م88/ج: `cloud_config.dart` (‏`googleWebClientId` من dart-define)،
`supabase_oauth_signin.dart` (المسار الأصلي + السقوط الرشيق)،
`cloud_boot.dart`، `post_login_gate.dart` (رابط الخروج)، `build_cloud.sh`
(تعريف `GOOGLE_WEB_CLIENT_ID`)، pubspec (‏`google_sign_in ^7.2.0`)،
وحارس minSdk في م83 حُدِّث (23→24). **الإجمالي بعد م88/ج: 723 اختباراً.**

---

## ٧) م91 — قفل الشاشة لداخل Google: «رمز قفل التطبيق»

كان توثيق م88 يقول: «مستخدم Google يفتح القفل بالبصمة أو بالخروج — وهذا
مقصود». **والوعد كان مكسوراً من طرفيه**: زرُّ البصمة نفسه كان مشروطاً
بوجود مُتحقِّق القفل (الذي لا يوجد لمسار OAuth أصلاً)، فكانت شاشة القفل
تعرض لداخل Google خيارَ «تسجيل الخروج» **فقط** — وقفلُ الإقلاع يتخطّى
صامتاً رغم مفتاح الإعدادات.

م91 سدّه بلا مساسٍ بمسار كلمة المرور ولا بالخادم ولا بالمزامنة:
- **رمز قفلٍ رقمي محلي** (4 أرقام فأكثر) تحت نفس مفتاح المُتحقِّق ونفس
  مغلّف PBKDF2 بملح، بوسم نوعٍ يفرّق (`password`/`pin`). يُقترح تعيينه
  **فور الدخول** بحوارٍ قابل للتخطي، ويُدار من الإعدادات (قسم قفل الدخول).
- **البصمة تعمل وحدها** بلا مُتحقِّق، وقفل الإقلاع يشمل حالة البصمة-فقط.
- تنبيهٌ صادق في الإعدادات عند «قفلٍ مفعَّلٍ بلا وسيلة فتح» + إرشادٌ في
  شاشة القفل نفسها، والخروج يبقى طريقاً سالكة دائماً.

التفصيل الكامل في `LATEST_CHANGES_AR.md`، والحارس
`test/m91_google_lock_pin_test.dart` (‏15 اختباراً).

---

## ٤) الملفات (م88 كاملة)

| الملف | التغيير |
|---|---|
| `supabase/migrations/0030_profiles.sql` | **جديد** — profiles + تريغر + RLS + backfill (مطبَّق حيّاً) |
| `lib/features/auth/oauth_signin.dart` | **جديد** — عقد OAuth + افتراض «غير مُفعَّل» |
| `lib/features/auth/supabase_oauth_signin.dart` | **جديد** — التنفيذ الحقيقي: deep-link أندرويد + loopback مكتبي |
| `lib/data/cloud/session_store.dart` | **جديد** — مخزن الجلسة الآمن + ترحيل `session.json` |
| `lib/data/cloud/supabase_boot.dart` | **جديد** — تهيئة الحزمة (PKCE) + مخازنها الآمنة + جسر الجلسة |
| `lib/app/cloud_boot.dart` | **جديد** — غراء الإقلاع السحابي (تدهور رشيق) |
| `lib/core/auth/auth_contracts.dart` | `displayName` + عقد `SdkSessionBridge` |
| `lib/data/cloud/supabase_auth_service.dart` | مقبسا المخزن والجسر + موزّع الجلسات |
| `lib/app/providers.dart` | مزوّدا المخزن والجسر + التوصيل |
| `lib/main.dart` | استدعاء الإقلاع السحابي قبل `runApp` |
| `lib/features/login/login_screen.dart` | زرّ «المتابعة باستخدام Google» |
| `lib/features/auth/post_login_gate.dart` | ترحيبٌ باسم Google للعائد |
| `android/.../AndroidManifest.xml` | `singleTask` + مرشّح نية `dentaldk://login-callback` |
| `android/app/build.gradle.kts` | ‏minSdk ‏23→24 (شرط `app_links`) |
| `pubspec.yaml` | ‏`supabase_flutter ^2.16.0` |
| `test/m88_google_signin_test.dart` | **جديد** — 6 اختبارات (الجسر والبوابة) |
| `test/m88_sdk_wiring_test.dart` | **جديد** — 17 اختباراً (المخازن والموزّع وloopback) |
| `test/m45_login_2026_test.dart` | تمريرٌ لزر التسجيل قبل النقر (البطاقة أطول) |

## ٥) Migrations / RLS / Auth — الخلاصة
- **Migration `0030_profiles`** مطبَّقة حيّاً. **RLS جديدة على `profiles`
  فقط**؛ لا تغيير على أي سياسة قائمة.
- **Auth**: مزوّد Google مفعَّل (المعرّف/السرّ في الخادم فقط) +
  `uri_allow_list` أعلاه. مفتاح الإدارة `sbp_` الذي استُعمل للضبط أوصى
  التقرير المالكَ بإلغائه (Revoke) فور الانتهاء.
