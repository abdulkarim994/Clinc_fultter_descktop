# 📘 دليل التطوير والرفع — عيادة الأسنان الرقمية
### كيف تعدّل المشروع بنفسك وترفعه إلى GitHub وتبني النسخ — خطوة بخطوة

> هذا الدليل يشرح **بالضبط** الآلية التي كنت أعمل بها، وكيف تكملها بنفسك على نفس المستودع:
> `github.com/abdulkarim994/Clinc_fultter_descktop`

---

## ١) ما الذي بين يديك الآن

| العنصر | مكانه |
|---|---|
| **المصدر الكامل** (آخر إصدار 2148 بكل الميزات) | ملف `dental_clinic_source_v2148_full.zip` المسلَّم في المحادثة |
| **حزمة الأسرار** (المفاتيح + المخزن) | عندك أصلاً — `cloud.env` و`dev_keystore.env` و`dental_dev.jks` |
| **المستودع** | يحوي حالياً: أجزاء الحمولة `_bootstrap/proj.b64.part00..04` + ملف البناء `.github/workflows/windows-build.yml` + أسرارك الستة في Secrets |
| **آخر APK** | 1.0.2148 (رقم البناء 2148 — أي بناء جديد يجب أن يكون **2149 فأعلى**) |

> ⚠️ **قاعدة ذهبية دائمة:** ملفات الأسرار (`*.jks`, `cloud.env`, `dev_keystore.env`) **لا تُرفع إلى GitHub أبداً** بأي شكل. ملف `.gitignore` في المشروع يحجبها تلقائياً — لا تتجاوزه. فقدان `dental_dev.jks` = استحالة تحديث التطبيقات المثبتة بنفس التوقيع نهائياً، فاحتفظ بنسخ آمنة منه.

---

## ٢) تجهيز بيئة التطوير على جهازك (مرة واحدة)

1. **Flutter 3.44.8 (قناة stable)** — حمّله من flutter.dev وأضفه إلى PATH.
   تحقق: `flutter --version`
2. **Android Studio** (أو أدوات SDK وحدها) — ثم من داخل المشروع: `flutter doctor` واقبل التراخيص:
   `flutter doctor --android-licenses`
3. **Git** — على ويندوز ثبّت **Git for Windows** (يعطيك أيضاً Git Bash لتشغيل سكربتات bash).
4. **فك ضغط المصدر** إلى مجلد عمل، مثلاً `C:\dental\src_app`.
5. **ضع الأسرار في أماكنها** (محلياً فقط):
   - `cloud.env` → انسخه إلى `src_app/tool/cloud.env`
   - `dental_dev.jks` → انسخه إلى `src_app/android/app/dental_dev.jks`
   - كلمتا المخزن من `dev_keystore.env` → تُصدَّران كمتغيّرات بيئة قبل البناء (انظر ٤).
6. **أول تشغيل:** `flutter pub get`

---

## ٣) دورة التعديل والاختبار

```bash
cd src_app
# ... عدّل الملفات التي تريد ...

flutter analyze          # يجب أن يكون صفر أخطاء
flutter test             # الحزمة الكاملة (~1075 اختباراً) — كلها خضراء قبل أي رفع
```

- على **ويندوز وماك** الاختبارات تعمل مباشرة (لا تلمس إعداد `hooks:` في `pubspec.yaml` — يجب أن يبقى `source: sqlcipher` دائماً، وهناك اختبار حارس `م83/ز` يفشل عمداً لو غيّرته وقت الشحن).
- اختبار سريع لملف واحد: `flutter test test/اسم_الملف.dart`
- بعد كل تعديل ناجح: احفظه في git محلياً:
  ```bash
  git add -A
  git commit -m "وصف التعديل"
  ```

**أين تجد الأشياء (خريطة سريعة):**
- شاشات الهاتف: `lib/features/…` (records = الرئيسية، patients = السجلات والملف، finance = المالية والخزينة والديون، appointments، labs، settings)
- شاشات الكمبيوتر: `lib/features/desktop/screens/…`
- المنطق المشترك (الحسابات): `home_logic.dart`، `treasury_logic.dart`، `patients_logic.dart` — **أي تعديل حسابي هنا يسري على المنصتين معاً**
- قاعدة البيانات والمزامنة: `lib/data/…` — **لا تعدّلها إلا لضرورة قصوى**

---

## ٤) بناء APK أندرويد موقّع (على جهازك)

من **Git Bash** داخل مجلد `src_app` (الأرقام من ملف `dev_keystore.env` عندك):

```bash
export DENTAL_DEV_STORE_PASSWORD='...كلمة المخزن...'
export DENTAL_DEV_KEY_PASSWORD='...كلمة المفتاح...'

./tool/build_cloud.sh trial 2149     # ← رقم البناء: زد 1 عن آخر نسخة دائماً
```

- `trial` = النسخة الموقّعة بمخزنك بهوية `com.dental.clinic.debug` (ترقّي المثبَّت مباشرة).
- الناتج في: `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` (هذا الذي توزعه).
- السكربت يقرأ `tool/cloud.env` تلقائياً ويحقن مفاتيح السحابة ويفعّل التعتيم.

---

## ٥) الرفع إلى GitHub + بناء نسخة ويندوز

عندك **طريقتان** — اختر واحدة:

### ⭐ المسار أ (الموصى به لك): الرفع المباشر الطبيعي
الأسهل والأوضح للعمل اليدوي المستمر: ترفع ملفات المشروع الحقيقية إلى المستودع بدل الأجزاء المشفّرة، فيصبح كل تعديل مستقبلي `commit + push` عادياً ويظهر تاريخ تعديلاتك ملفاً ملفاً.

**التحويل (مرة واحدة):**
1. من مجلد `src_app` اربط المستودع وادفع كل شيء:
   ```bash
   git remote add origin https://github.com/abdulkarim994/Clinc_fultter_descktop.git
   git push -u origin main --force
   ```
   (`--force` لأن تاريخ المستودع الحالي أجزاء حمولة فقط — سيُستبدل بمصدرك الحقيقي. أسرارك في Secrets **لا تتأثر**.)
2. استبدل محتوى `.github/workflows/windows-build.yml` بمحتوى الملف المرفق **`windows-build-v4-direct.yml`** (يبني من الملفات مباشرة — لا إعادة تجميع).
3. من الآن فصاعداً، كل تعديل:
   ```bash
   git add -A && git commit -m "وصف" && git push
   ```
   **وسيبدأ بناء ويندوز تلقائياً مع كل push** (أو يدوياً من Actions ← Run workflow). النسخة تُحمَّل من قسم Artifacts.

### المسار ب: نفس آليتي الحالية (أجزاء base64)
إن أردت إبقاء المستودع كما هو (بلا مصدر مكشوف مباشرة):
1. بعد كل `git commit` محلي، شغّل السكربت المرفق:
   - على ويندوز (PowerShell): `.\make_payload.ps1 -Src "C:\dental\src_app"`
   - على لينكس/ماك أو Git Bash: `./make_payload.sh /path/to/src_app`
   ينتج مجلد `payload/` فيه 5 ملفات `proj.b64.part00..04` + بصمة تحقق، **ويفحص تلقائياً** خلوّها من الأسرار.
2. استبدل الأجزاء الخمسة في `_bootstrap/` بالمستودع:
   - **من المتصفح:** افتح كل ملف جزء ← ✏️ ← Select All واحذف ← الصق محتوى الجزء الجديد ← Commit. (كرر للخمسة — النص كبير، الصبر مطلوب.)
   - **أو بـ git** (أسهل): استنسخ المستودع، انسخ الأجزاء الخمسة فوق القديمة، `commit + push`.
3. شغّل البناء يدوياً: Actions ← «بناء نسخة ويندوز (Bootstrap)» ← **Run workflow**.

> في الحالتين: **workflow البناء يقرأ أسرارك الستة من Settings → Secrets** (موجودة عندك) ويخبزها داخل النسخة — لا تكتبها في أي ملف يُرفع.

---

## ٦) قواعد لا تُكسر (ملخص السلامة)

1. **لا أسرار في المستودع** — أبداً. الحارس في workflow يفشل البناء لو تسللت.
2. **رقم البناء يتصاعد دائماً** (2149، 2150…) وإلا رفض أندرويد الترقية.
3. **`pubspec.yaml`:** سطر `source: sqlcipher` لا يتغيّر (تشفير قاعدة البيانات).
4. **قبل أي رفع:** `flutter analyze` صفر + `flutter test` أخضر.
5. **المنطق المشترك** (`home_logic`, `treasury_logic`, `patients_logic`) يخدم الهاتف والكمبيوتر معاً — عدّله بحذر واختبر المنصتين.
6. الكتابة للبيانات تمرّ دائماً عبر `upsertLocal` (يختم التعديل للمزامنة) — **لا تستعمل `update()`** للتحرير من الواجهة (لا يتزامن؛ استعمل `updateLocal`).
7. المستودع عام حالياً — يمكنك جعله Private من Settings متى شئت (البناء والأسرار يعملان كالمعتاد).

---

## ٧) مرجع سريع — أوامر تحتاجها كثيراً

```bash
flutter pub get                    # تثبيت الاعتماديات
flutter analyze                    # فحص الأخطاء
flutter test                       # كل الاختبارات
flutter test test/analyses_test.dart   # اختبار محدد
./tool/build_cloud.sh trial 2149   # APK موقّع موصول بالسحابة
flutter build windows --release    # ويندوز محلياً (على جهاز ويندوز، أضف --dart-define للمفاتيح كما في workflow)
git add -A && git commit -m "..."  # حفظ محلي
git push                           # رفع (المسار أ)
```

---

## ٨) حالة المشروع لحظة التسليم (7 أغسطس 2026)

- آخر commit: `71ba6d7` — نظام التحاليل + عزل هوية المرضى + إصلاح مزامنة المعلومات المختصرة.
- الاختبارات: **1075 خضراء**، analyze صفر، مراجعة نهائية بلا ملاحظات.
- آخر حمولة مرفوعة على GitHub مطابقة لهذا المصدر بايتاً ببايت (sha256: `576260bd…`).
- كل ما أنجز في هذه الجلسات موثّق في رسائل المحادثة — والتقارير المفصلة (تقرير التكافؤ، تقرير الأمان) منشورة فيها.

بالتوفيق 🦷 — المشروع بين يديك الآن كاملاً.
