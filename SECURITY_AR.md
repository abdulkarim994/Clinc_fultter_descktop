# دليل الأمن — ما نُفِّذ وما يتطلب فعلك

> **تنبيه سياق (2026-07-31):** أربعة بنود كانت مُدرجة هنا نواقصَ — تدوير
> مفتاح `anon`، وإغلاق التسجيل الذاتي، وتأكيد البريد، وحماية كلمات المرور
> المسرَّبة — **خرجت من حساب النواقص بقرار المالك**، بعد التحقق بالتشغيل
> أن عزل البيانات مفروضٌ داخل دوال المزامنة بـ`auth.uid()` لا بإخفاء
> المفتاح. الأدلّة في قسم «قرار المالك» من `SECURITY_RUNBOOK_AR.md`.
>
> والمشروع في **مرحلة البناء بلا بيانات مرضى حقيقية** — وهو ما يجعل هذا
> التقدير سليماً اليوم، ويستدعي مراجعته عند النشر العام.


> م68/دفعة ثانٍ. هذه الوثيقة تفصل بصراحة بين ما أُصلِح في الشيفرة ومُختبِر،
> وما **لا يمكن** إصلاحه بالشيفرة وحدها، وما تُرك عمداً لأنه غير قابل
> للتحقق في بيئة الاختبار.

---

## القسم أ — نُفِّذ ومُختبَر (473 اختباراً خضراء)

| # | الإصلاح | الموضع | الإثبات |
|---|---|---|---|
| 1 | PBKDF2-HMAC-SHA256 بملح لكل حساب بدل SHA-256 العارية | `lib/features/auth/password_hash.dart` | 11 اختباراً، منها مطابقة **متجه مرجعي معياري** |
| 2 | ترحيل شفاف للحسابات القديمة عند أول دخول ناجح | `auth_service.dart` | اختبار يزرع هاشاً قديماً ويؤكد ترقيته |
| 3 | خنق محاولات الدخول الفاشلة (قفل مؤقت) | `auth_service.dart` | اختبار |
| 4 | رفع الحد الأدنى لكلمة المرور إلى 8 | `auth_service.dart` | اختبار |
| 5 | مسح هاشات المرور عند تبديل الحساب | `onboarding_service.dart` | اختبار |
| 6 | «تذكّرني=لا» يمحو جلسة الحساب السابق | `supabase_auth_service.dart` | 3 اختبارات |
| 7 | إخراج أسرار Supabase من سكربتَي البناء | `tool/build_cloud.*` | فحص نصي |
| 8 | تعطيل النسخ الاحتياطي + قواعد استثناء | `AndroidManifest.xml` + `res/xml/` | تحقق XML |
| 9 | أسرار التوقيع خارج المستودع، وفشل release بلا مفتاح | `build.gradle.kts` | مراجعة |
| 10 | تعتيم رموز Dart في نسخ الإصدار | `tool/build_cloud.*` | مراجعة |

### تحذير على البند 9
تغييرات Gradle **لم تُنفَّذ فعلياً** هنا — بيئة الاختبار بلا Android SDK،
فتحققي منها كان بالمراجعة لا بالبناء. **ابنِ نسخة debug ونسخة trial مرة
واحدة قبل الاعتماد** للتأكد من أن قراءة `key.properties` تعمل عندك.

---

## القسم ب — يتطلب جهازك: تشفير القاعدة ونقل الجلسة

**لم أشحن هذين عمداً.** السبب ليس صعوبتهما بل أنني **لا أستطيع التحقق منهما
هنا**، وشحن شيفرة تشفير غير مُختبَرة على قواعد عيادات عاملة قد يجعل فتحها
مستحيلاً — خسارة لا رجعة فيها.

### لماذا يتعذّر التحقق في بيئة الاختبار
- `flutter test` يعمل على المضيف بـ `sqlite3` العادي — **لا sqlcipher**. أي
  اختبار «تشفير» هنا سيمرّ بينما القاعدة على القرص نصٌّ صريح.
- التخزين الآمن للنظام يحتاج قنوات منصّة (Keystore/DPAPI) غير متاحة في
  اختبارات المضيف.

### خطوات التنفيذ عند الرغبة

**1) تشفير القاعدة**
```yaml
# pubspec.yaml — استبدال، لا إضافة
- sqlite3_flutter_libs: ^0.6.0+eol
+ sqlcipher_flutter_libs: ^0.6.0
```
```dart
// lib/data/db/bootstrap.dart — قبل أي DDL
final db = sqlite3.open(path);
db.execute("PRAGMA key = '$dbKey';");   // أول أمر بلا استثناء
db.execute('PRAGMA journal_mode = WAL;');
```

**2) مفتاح القاعدة** — يُولَّد مرة عشوائياً ويُحفظ في تخزين النظام الآمن
(`flutter_secure_storage`): Keystore على أندرويد، DPAPI على ويندوز.
لا تشتقّه من شيء ثابت ولا تضعه في الشيفرة.

**3) ترحيل القواعد القائمة — الجزء الحرج**
قاعدة غير مشفّرة لا تُفتح بمفتاح. يلزم ترحيل صريح:
```sql
ATTACH DATABASE 'enc.db' AS enc KEY '<المفتاح>';
SELECT sqlcipher_export('enc');
DETACH DATABASE enc;
```
ثم استبدال الملف الأصلي. **اختبر هذا على نسخة من قاعدة حقيقية أولاً**،
واحتفظ بنسخة احتياطية قبل الاستبدال، واجعل الترحيل قابلاً للاستئناف إن
انقطع في المنتصف.

**4) نقل الجلسة** — استبدال `session.json` بـ `flutter_secure_storage`، مع
حذف الملف القديم بعد النقل الناجح.

**معيار القبول:** أن يكون أول 16 بايت من ملف القاعدة **ليس** `SQLite format 3`،
وأن تُفتح القاعدة بعد إعادة التشغيل، وأن تنجو بيانات قاعدة قائمة عبر الترحيل.

---

## القسم ج — أفعال بيدك لا تملكها الشيفرة

هذه **ليست مهام برمجية**. لا سطر كود ينجزها:

1. **تدوير مفتاح Supabase** — لوحة Supabase → Settings → API → Rotate.
   المفتاح الذي كان في سكربتات البناء **مكشوف**، وإزالته من الملفات لا
   تُبطله: هو باقٍ في تاريخ git وفي كل نسخة أرشيف وُزّعت.
   ودوّر **سر JWT** أيضاً لإبطال أي رمز صادر.

2. **توليد مخزن توقيع جديد** — المفتاح القديم محروق. راجع
   `android/key.properties.example`. **احفظ نسخة احتياطية**: فقدانه يعني
   استحالة ترقية النسخ المنصَّبة.

3. **تفعيل تأكيد البريد** وتقييد التسجيل — الوثيقة كانت تنصح بتعطيله
   للتجربة، وهو ما يسمح لأي شخص بالتسجيل الذاتي والوصول إلى واجهة
   المشروع بصفة مستخدم مصادَق.

4. **تفعيل الحماية من كلمات المرور المسرَّبة** — لوحة Supabase →
   Authentication → Policies. يقارن Supabase كلمة المرور بقاعدة
   HaveIBeenPwned عند التسجيل. **إعداد لوحة لا SQL** — لا مهاجرة تفعّله.
   هذا آخر تحذير أمني متبقٍ لا يملكه المستودع.

> البند الذي كان هنا عن **مراجعة سياسات RLS** أُنجز — انظر القسم د.

---

## القسم د — تقوية قاعدة الإنتاج (طُبِّقت ومُثبَتة)

أربع مهاجرات طُبِّقت على قاعدة الإنتاج وحُفظت في `supabase/migrations/`
لئلا تبقى حالة القاعدة خارج المستودع:

| # | الملف | ما عالجته |
|---|---|---|
| 0014 | `harden_function_search_path.sql` | تثبيت `search_path` على 17 دالة — أغلق باب خطف SECURITY DEFINER |
| 0015 | `revoke_public_execute_security_definer.sql` | سحب `EXECUTE` من PUBLIC (لا من `anon` — انظر أدناه) |
| 0016 | `rls_initplan_and_policy_dedupe.sql` | `(SELECT auth.uid())` بدل `auth.uid()`، ودمج 9 سياسات ← 4 |
| 0017 | `revoke_api_grants_on_partitions.sql` | إخراج 64 شريحة من سطح PostgREST |

**الأثر المرصود على مدقّقي Supabase**

| المدقّق | قبل | بعد |
|---|---|---|
| الأداء | 36 تحذيراً | **0** |
| الأمن | 20 تحذيراً | **3** |

الثلاثة الباقية: اثنان مقصودان (`purge_expired` و`purge_rows` يستدعيهما
التطبيق بهوية مصادَقة وكلاهما مقيَّد بـ `user_id = auth.uid()`)، والثالث
هو حماية كلمات المرور المسرَّبة أعلاه — إعداد لوحة لا SQL.

### درسان يستحقان التسجيل

**`REVOKE` قد ينجح ولا يفعل شيئاً.** أول محاولة كانت `REVOKE ... FROM anon`
وأعادت `success`، والتحقق بعدها أظهر `anon_can_exec = true` كما كان. قائمة
التحكم بالوصول كشفت السبب: `=X/postgres` — بلا اسم دور قبل `=X`، أي منحة
لـ **PUBLIC**. و`anon` كان ينفّذ بعضويته الضمنية فيها لا بمنحة باسمه.
**لا تثق بـ `success` من `REVOKE`؛ تحقّق بـ `has_function_privilege`.**

**الشرائح كانت مكشوفة لكن مغلقة.** الـ64 شريحة كانت نقاط نهاية REST صالحة،
لكن قراءة `sync_rows_p61` مباشرةً بهوية صاحب البيانات نفسه أعادت **0** بينما
الجدول يحوي 149,006 صفاً — لأن «RLS مفعَّل بلا سياسة» يعني منعاً كاملاً.
لم يكن تسريباً، لكن الأمان كان معلَّقاً على طبقة واحدة: أي تعطيل عرضي لـ RLS
مستقبلاً كان سيفتح الجدول كاملاً بلا شيء يعترضه. الآن الفشل مغلق على طبقتين.

### الإثبات السلبي للعزل

إعادة كتابة سياسة `SELECT` على المسار الساخن تستوجب إثباتاً. بعد التطبيق،
بهويتين حقيقيتين من الإنتاج:

```
user1 يرى صفوفه        : 149,006
user1 يرى صفوف user2   : 0        ← المطلوب
user2 يرى صفوفه        : 325
anon يرى               : 0        ← المطلوب
مجموع الصفوف           : 149,892  (لم يتغيّر)
```

**ويبقى مطلوباً منك:** نقل هذا الاختبار السلبي إلى CI. الإثبات أعلاه لقطة
لحظية؛ ما يمنع الانحدار هو اختبار يعمل مع كل تغيير، لا نتيجة محفوظة في وثيقة.

---

## مخاطر متبقية معروفة (لم تُصلَح)

| الخطر | الحالة | لماذا |
|---|---|---|
| **مفتاح Supabase المكشوف ما زال صالحاً** | **قائم — الأشد** | إخراجه من الملفات لا يُبطله؛ هو في تاريخ git وفي كل APK وُزِّع. **لا يُغلق إلا بتدويره بيدك** (القسم ج/1). تقوية القاعدة أعلاه تحدّ مما يفعله حامله، لا تُبطله |
| قاعدة المرضى غير مشفّرة | **قائم** | القسم ب — يتطلب جهازك |
| `session.json` نصّي | **قائم** | القسم ب |
| أسماء المرضى في مفاتيح تخزين R2 | **قائم** | يتطلب ترحيل كائنات R2 القائمة |
| نسخة trial موقَّعة بمفتاح تطوير معروف | **قائم جزئياً** | تُوزَّع فعلاً؛ استعمل مفتاحاً حقيقياً لها أيضاً إن وُزّعت خارج فريقك |
| لا تجريد EXIF من صور الأشعة | **قائم** | خارج نطاق هذه الدفعة |
| بيانات المرضى على ويندوز بلا صندوق رملي | **قائم** | يخفّفه تشفير القاعدة (القسم ب) |

> بيانات المرضى بيانات صحية. تعطيل النسخ الاحتياطي (البند 8) أوقف أكبر
> قناة تسريب مستمرة، لكن **التشفير عند السكون يبقى غير منجَز** — وهو أهم
> بند متبقٍ في هذا الملف.

---

## ملحق م91 (2026‑08‑01) — قفل الشاشة لمسار Google

| # | الإصلاح | الموضع | الإثبات |
|---|---|---|---|
| 11 | «رمز قفل التطبيق» لداخل Google: مُتحقِّق PBKDF2 بملح تحت نفس مفتاح مُتحقِّق القفل، بوسم نوعٍ (`password`/`pin`)، محلي لا يُزامَن ويُمسح مع تبديل الحساب والخروج | `idle_lock.dart` + `pin_setup.dart` | 15 اختباراً (`m91`) |
| 12 | البصمة تعمل وحدها بلا مُتحقِّق (كانت محجوبة عمّن يحتاجها)، وقفل الإقلاع يشمل حالة البصمة-فقط بدل التخطي الصامت | `idle_lock.dart` + `post_login_gate.dart` | اختبارات `m91/ب` |
| 13 | خنق محاولات الفتح على غطاء القفل (كان بلا أي خنق): 5 إخفاقات ⇒ 30 ثانية، في الذاكرة كنمط البند 3 | `idle_lock.dart` | اختبار `m91/و` |
| 14 | مصارحة الإعدادات: تنبيهٌ حين يكون القفل مفعَّلاً بلا أي وسيلة فتح + زر تعيينٍ فوري | `settings_screen.dart` | اختبارات `m91/هـ` |

> ملاحظة تهديد صريحة: رمزٌ من 4 أرقام فضاؤه 10⁴ — PBKDF2 لا تجعله عصياً
> على كسرٍ **خارج التطبيق** لمن نسخ القاعدة، فهو **قفلُ شاشةٍ** فوق بياناتٍ
> مشفَّرة أصلاً (م83) لا بديلاً عن كلمة مرور. داخل التطبيق يحدّه الخنق
> (البند 13)، ونصُّ الحوار ينصح بستة أرقام.
