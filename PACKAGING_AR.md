# دليل التغليف والترحيل (ويندوز + أندرويد) — م5ب

## الهوية المعتمدة

| | القيمة | لماذا |
|---|---|---|
| معرّف أندرويد (applicationId/namespace) | `com.dental.clinic` | **نفس معرّف تطبيق Capacitor الأصلي** — تثبيت الترقية يُبقي مجلد بيانات التطبيق نفسه فتقرأ أداة الترحيل قاعدة القديم مباشرة من `databases/dental_clinic_offlineSQLite.db` |
| اسم التطبيق (أندرويد + نافذة ويندوز + MSIX) | «طب الأسنان الرقمي» | نفس اسم الأصل |
| أيقونات المشغّل | مولدة من `assets/icon/icon-512.png` (أيقونة الأصل) عبر `flutter_launcher_icons` — mipmaps أندرويد + `app_icon.ico` ويندوز | `dart run flutter_launcher_icons` لإعادة التوليد |
| إذن الشبكة | `INTERNET` في المانيفست الرئيس | إلزامي لوضع السحابة في نسخ release |

## أندرويد — البناء والتوقيع

```bash
flutter build apk --release          # أو appbundle للمتجر
```

**نقطة حاسمة — التوقيع:** استخدام نفس `applicationId` يعني أن أندرويد لن
يقبل التثبيت فوق تطبيق Capacitor المنصّب **إلا بنفس مفتاح التوقيع
(keystore) الأصلي**:

- **تملك keystore الأصلي** ← أنشئ `android/key.properties` ووقّع به
  (استبدل كتلة `signingConfig` القياسية في `android/app/build.gradle.kts`
  بالإعداد المعروف من وثائق Flutter). التثبيت يرقّي القديم في مكانه،
  وشاشة «استيراد من التطبيق القديم» تكتشف قاعدته تلقائياً.
- **لا تملكه** ← غيّر `applicationId` (مثلاً `com.dental.clinic.flutter`)
  ليُنصَّب جنباً إلى جنب، ورحّل القاعدة بمنتقي الملفات (انسخ ملف القاعدة
  من جهاز مروّت أو عبر نسخة احتياطية يصدّرها التطبيق القديم).

الافتراضي الحالي في `buildTypes.release` هو توقيع debug (كما يولده قالب
Flutter) — **لا تنشر به**؛ هو للتجربة فقط.

## ويندوز — البناء والحزمة

```bash
flutter build windows --release
dart run msix:create        # يولّد حزمة MSIX من msix_config في pubspec.yaml
```

- عنوان النافذة عربي («طب الأسنان الرقمي») والأيقونة مولدة.
- `msix_config`: الهوية `com.dental.clinic` والاسم المعروض عربي واللغتان
  ar/en وقدرة `internetClient`. للنشر خارج المتجر وقّع الحزمة بشهادة
  (`certificate_path`/`certificate_password` في نفس الكتلة) أو وزّع مجلد
  `build/windows/x64/runner/Release` كما هو مع تثبيت يدوي.
- للبناء بوضع سحابي مضمّن وقت الترجمة:

```bash
flutter build windows --release \
  --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=... \
  --dart-define=R2_WORKER=...
```

> **حدود بيئة التطوير هنا:** إعدادات المنصتين والأيقونات والاختبارات كلها
> مهيأة ومختبرة على مستوى الملفات، لكن `flutter build windows/apk` نفسه
> يتطلب جهاز ويندوز/أدوات أندرويد — غير متاحين في بيئة لينكس هذه. أوامر
> البناء أعلاه جاهزة للتشغيل على جهازك.

## أداة الترحيل — ⚠ حُذفت من التطبيق (م64)

> **هذا القسم يصف ميزة لم تعد موجودة.** أُزيلت مجموعة «السحابة والترحيل»
> من شاشة الإعدادات في المعلم 64 (انظر `settings_screen.dart` والاختبار
> `test/m5b_packaging_migration_test.dart` الذي يوثّق الحذف). فمن يتبع
> الخطوات أدناه **يبحث عن قائمة غير موجودة**.
>
> **البديل الحالي** لنقل عيادة من نسخة Vue: انسخ ملف القاعدة القديم يدوياً
> إلى مجلد بيانات التطبيق قبل أول تشغيل، أو أعد إدخال البيانات عبر
> المزامنة من حساب Supabase نفسه. راجع `SETUP_SUPABASE_AR.md`.
>
> يبقى الوصف أدناه **مرجعاً تاريخياً** لمن يريد إعادة بناء الأداة: منطق
> إعادة ختم الملكية وتصفير حالة المزامنة ما زال صحيحاً وقابلاً لإعادة
> الاستعمال.

<details>
<summary>الوصف التاريخي للأداة المحذوفة</summary>

من **الإعدادات ← استيراد من التطبيق القديم** (يلزم تسجيل الدخول أولاً):

1. **اكتشاف تلقائي** لمسارات قواعد القديم المعروفة:
   - ويندوز: `%APPDATA%\Dental Clinic\dental_clinic_offline.db`
   - أندرويد (نفس المعرّف): `databases/dental_clinic_offlineSQLite.db`
   - أو اختيار الملف يدوياً.
2. **تقرير فحص للقراءة فقط**: إصدار المخطط، عدد الصفوف لكل جدول، المالك
   القديم — قبل أي كتابة.
3. **تنفيذ بتأكيد**: نسخة احتياطية تلقائية من أي قاعدة قائمة، ثم نسخ
   قاعدة القديم، ثم:
   - إعادة ختم `owner_uid` لكل الجداول ← حسابك الحالي (بيانات القديم
     كانت بمعرّف مشروع Supabase القديم).
   - `_dirty=1` وserver_seq=NULL لكل الصفوف ← أول مزامنة تدفع **كل**
     البيانات لمشروعك الجديد.
   - تصفير حالة المزامنة (مؤشر السحب/المحاولات/الحجر/آخر مزامنة).
   - **جهاز منطقي جديد** (device_id جديد + تصفير ساعة HLC) — شرط صحة
     التشغيل المتوازي مع تطبيق Vue على نفس الجهاز.
   - حمل حسابات/جلسة المصادقة المحلية (الوضع المحلي) كي لا تُطرد من الدخول.
4. بعد العودة: اضغط زر «مزامنة» لدفع كل شيء للسحابة الجديدة.

**ما لا يرحَّل:** بايتات صور الأشعة الكاملة (المصغرات داخل القاعدة
تُرحَّل؛ الكاملة تُستعاد من R2 متى فُعّل، أو تبقى على أجهزتها) — وملفات
PDF المصدّرة.

</details>
