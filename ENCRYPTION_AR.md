# التشفير عند السكون — م83

> **اقرأ قسم «ما تغيّر في النسخ الاحتياطي» قبل أي شيء آخر.** التشفير يغيّر
> قواعد استعادة البيانات تغييراً جذرياً، ومن لا يعرف ذلك قد يفقد عيادةً
> كاملةً وهو يظنّ أنه يحتفظ بنسخة.

---

## ١) ما الذي صار مشفَّراً

| البيان | قبل | بعد |
|---|---|---|
| قاعدة المرضى والمواعيد والسجلات والديون | ملف SQLite **مقروء** | SQLCipher (AES‑256) |
| ملف WAL المرافق | مقروء | مشفَّر (تُشفَّر إطاراته) |
| صور الأشعة والمصغّرات | ملفات `.jpg` **مقروءة** | خزنة SQLCipher (`xray_vault.db`) |
| مفتاح التشفير | — | مخزن النظام: Keystore / DPAPI |

**وما لم يُشفَّر — بصراحة:**

| الملف | لماذا | أيحوي بيانات مريض؟ |
|---|---|---|
| `cloud_config.json` | إعداد اتصال، يُقرأ قبل حسم المفتاح | لا |
| `session.json` | رمز جلسة | لا (بريد المستخدم فقط) |
| `errors.log` | سجل أعطال، يمرّ بمنقّح PHI | لا (مُنقّح) |
| `exports/` | ما يُصدّره المالك عمداً | **نعم** — مسؤولية المالك |

ما يُصدَّر إلى `exports/` يخرج من حماية التطبيق بالتعريف. عامله معاملة
ورقةٍ مطبوعة فيها اسم مريض.

---

## ٢) ما تغيّر في النسخ الاحتياطي — الأهمّ

قبل هذه النسخة كانت نسخةُ مجلد البيانات كافية للاستعادة. **لم تعد.**

المفتاح يعيش في مخزن أسرار النظام — مربوطاً بالجهاز وبحساب المستخدم — ولا
يُنسخ مع الملفات. أي أن:

> **نسخُ ملف القاعدة إلى قرص خارجي أو سحابة ينتج ملفاً لا يفتحه أحد،
> ولا أنت.**

وهذا ليس عطلاً بل **تعريف التشفير**. ولو كان المفتاح ينتقل مع الملف لما
كان هناك تشفير أصلاً.

### إذاً كيف تحمي بياناتك؟

**١) المزامنة السحابية هي نسخُك الاحتياطي الحقيقي.** فعِّلها وتحقّق أنها
تعمل. البيانات على الخادم مستقلّة عن مفتاح الجهاز، ولوحٌ مسروق أو حاسوب
محترق لا يفقدك شيئاً: تُسجّل الدخول على جهاز جديد فيسترجع.

**٢) على أندرويد:** نسخُ Google الاحتياطي **مُعطَّل عمداً**
(`allowBackup="false"`). ولو تُرك مفعَّلاً لنُسخت القاعدة المشفَّرة إلى
حساب Google **بلا مفتاحها** — فتُستعاد على جهاز جديد ملفاً لا يُفتح، وهو
أسوأ من ألّا تُنسخ: يمنح إحساساً كاذباً بالأمان.

**٣) على ويندوز:** انتقلت البيانات من `Roaming` إلى `Local`. والسبب أن
`Roaming` في شبكات النطاق تُنسخ إلى خادم الشبكة عند كل تسجيل خروج — أي
نسخُ قاعدة مرضى إلى خادم لا يعلم به المالك، مع خطر تلف SQLite على مسار
يُزامَن خلف ظهر التطبيق. النقل يتم **تلقائياً مرّة واحدة** عند أول تشغيل،
ويُترك في المكان القديم ملف `MOVED_TO.txt` يدلّ على الوجهة.

---

## ٣) ماذا يحدث عند الترقية من نسخة سابقة

عند أول تشغيل بعد التحديث، وقبل ظهور أي شاشة:

1. **يُنقل مجلد البيانات** (ويندوز فقط) من `Roaming` إلى `Local`.
2. **يُولَّد مفتاح** ويُحفظ في مخزن النظام.
3. **تُرحَّل القاعدة**: نقطةُ تفتيش لسجل WAL ← نسخة احتياطية سادة ←
   تصدير مشفَّر ← **تحقّق بالفتح والقراءة** ← إبدال ذرّي.
4. **تُنقل صور الأشعة** إلى الخزنة، كلٌّ بعد التحقق من وصولها.
5. عند **الإقلاع التالي** — بعد أن تُثبت المشفَّرة أنها تعمل — تُحذف
   النسخة السادة تلقائياً.

الترتيب مصمَّم بحيث **لا لحظة** يكون فيها الأصل محذوفاً وبديلُه غير
مُتحقَّق منه. وأي إخفاق قبل الإبدال يترك ملفك **سليماً بايتاً بايتاً**،
والتطبيق يعرض شاشةً عربية تشرح ما جرى بدل بياضٍ صامت.

> **لماذا تُحذف النسخة السادة تلقائياً؟** لأنها تفتح بلا مفتاح وتحوي كل
> شيء. نسخةٌ سادة بجوار قاعدة مشفَّرة **تُبطل التشفير عملياً** — الأمر
> أشبه بقفل الباب وترك المفتاح في القفل. ولا تُحذف قبل أن تُفتح المشفَّرة
> بالمفتاح ويُقرأ منها جدولُ بيانات فعلاً.

---

## ٤) «تعذّر فتح قاعدة البيانات» — ماذا تفعل

تظهر هذه الشاشة حين تكون القاعدة مشفَّرة والمفتاح غير متاح. **بياناتك لم
تُحذف** — التطبيق يرفض عمداً توليد مفتاح جديد، لأن ذلك يُنشئ قاعدةً فارغة
تبدو كأن العيادة تبخّرت، وهو أسوأ فشل ممكن: صامتٌ ولا رجعة فيه.

| السبب المحتمل | الحل |
|---|---|
| جهاز جديد | سجّل الدخول بحسابك — تسترجع من السحابة |
| استعادة نسخة نظام لم تشمل مخزن الأسرار | أعد الاستعادة كاملةً |
| مسح بيانات التطبيق من إعدادات النظام | المفتاح ذهب معها — استرجع من السحابة |
| تغيّر مفتاح توقيع التطبيق (أندرويد) | ثبّت النسخة الموقَّعة بالمفتاح نفسه |

**لا تحذف التطبيق قبل أن تسترجع.** الحذف يمحو الملف المشفَّر أيضاً.

---

## ٥) للمطوّر

### إعداد البناء — لا تلمسه

```yaml
hooks:
  user_defines:
    sqlite3:
      source: sqlcipher
```

**السبب أن SQLite العادية تتجاهل `PRAGMA key` صامتةً** — بلا خطأ ولا
تحذير. فلو شُحنت مكتبة بلا SQLCipher لمضى التطبيق يكتب أسماء المرضى نصّاً
مقروءاً وهو موقنٌ أنه يشفّر، **وكلُّ الاختبارات تمرّ** لأنها تُشغَّل على
بيئة صحيحة. ولذلك حارسان:

1. **اختبار `م83/ز`** يفحص هذا السطر نفسه (متجاهلاً التعليقات).
2. **فحص `PRAGMA cipher_version` وقت التشغيل** في `openBootstrappedDb`
   وفي خزنة الأشعة — يوقف الإقلاع بدل الكتابة نصّاً.

### على توزيعة لينكس قديمة

المكتبة الجاهزة تتطلّب glibc ≥ 2.38. وعلى أقدم منها تفشل بـ
`GLIBC_2.38 not found`. الحلّ للتطوير المحلي فقط:

```yaml
      source: system
      name_linux: /usr/local/lib/libsqlcipher.so
```

**وأعد السطر قبل أي بناء إصدار** — الاختبار `م83/ز` يذكّرك، وهو يستبعد
التعليقات كي لا يصير أحمر دائماً بلا فائدة.

### إيقاف التشفير للتطوير

```
flutter run --dart-define=DB_PLAINTEXT=true
```

علمٌ **صريح** يظهر في أمر البناء، لا ارتدادٌ صامت. ولا يعمل فوق قاعدة
مشفَّرة قائمة — يعرض رسالةً بدل انهيار غامض.

### الخريطة

| الملف | المسؤولية |
|---|---|
| `db_key.dart` | توليد المفتاح، صيغة `PRAGMA`، دورة الحياة |
| `secure_key_store.dart` | Keystore / DPAPI — و`resetOnError: false` |
| `db_encrypt.dart` | التصنيف، الترحيل، الاسترجاع، حذف الطوق |
| `db_paths.dart` | نقل ويندوز من Roaming إلى Local |
| `db_boot.dart` | المتتالية كاملةً — وكلُّ فروعها مُختبَرة |
| `blob_vault.dart` | خزنة صور الأشعة |
| `bootstrap.dart` | ترتيب `PRAGMA key` وحارس المكتبة |

### إعدادٌ لا يُعاد إلى افتراضه

`SecureDbKeyStore` يمرّر **`resetOnError: false`**. وافتراض الحزمة `true`
يعني حرفياً: «أخفق فكُّ التشفير ⇒ احذف كل البيانات للتعافي». وهو تصرّفٌ
معقول لرمز جلسة يُعاد جلبه، **وكارثة مطلقة** لمفتاح قاعدة: عطلٌ عابر في
Keystore — بعد تحديث نظام أو استعادة نسخة — يمحو المفتاح فتصير قاعدة
العيادة كتلةً لا يفتحها أحد، بلا رسالة ولا رجعة.

---

## ٦) حدود ما جرى التحقق منه

**مُتحقَّق منه بالتشغيل** (٥٣ اختباراً في `m83_phase6_encryption_test.dart`):
الملف مشفَّر على القرص فعلاً واسم المريض غير موجود في بايتاته؛ المفتاح
الخاطئ يُرفض؛ الترحيل ينقل البيانات ويتحقق قبل الإبدال؛ الانقطاع في نافذة
الإبدال يُستعاد ولا يُنشئ قاعدة فارغة؛ فقدان المفتاح لا يغيّر الملف بايتاً
واحداً؛ العلامة المفقودة لا تُتلف قاعدةً سليمة والشاردة لا تُقفل تطبيقاً
فارغاً؛ صور الأشعة ومحتواها **وأسماء مدخلاتها** غير مقروءة؛ نقلُ مجلد
ويندوز ينقل الشجرة كاملةً وهو عديم الأثر.

**غير مُتحقَّق منه هنا — ويحتاج جهازاً حقيقياً:**

- سلوك **Keystore على أندرويد** و**DPAPI على ويندوز** فعلياً. الواجهة
  مُختبَرة بالكامل عبر مخزن في الذاكرة، والتنفيذ المنصّي رقيقٌ عمداً
  (نحو عشرة أسطر) — لكن «رقيق» ليس «مُتحقَّقاً منه».
- **قفلُ الملفات على ويندوز**: صُمِّم له (سجلّ الخزنات، الإبدال الذرّي،
  الاسترجاع) ولم يُشغَّل على ويندوز.
- **أداءُ الترحيل على قاعدة كبيرة**: `sqlcipher_export` يعيد كتابة كل
  صفحة. قاعدة بحجم غيغابايت تستغرق دقائق عند أول تشغيل.

> **سياق:** كُتب هذا والمشروع في مرحلة البناء بلا بيانات مرضى حقيقية،
> فأثرُ أي إخفاق اليوم محدود. والتصميم أعلاه مكتوبٌ **لأجل ما بعد ذلك** —
> ترحيلُ قاعدة عيادةٍ عاملة لا يُعاد تصميمه وقتها.
