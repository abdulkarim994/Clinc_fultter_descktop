# تنظيف قاعدة الإنتاج — ما حُذف، وما لا يجوز حذفه، وما ينتظر قرارك

> م69 · 2026-07-30 · مشروع `<PROJECT-REF>`
>
> الخلاصة مقدَّماً: **القاعدة نظيفة أكثر مما تبدو.** أكثر ما يبدو مكرراً
> أو ميتاً هو في الحقيقة حامل حِمل، واثنان مما بدا «آمناً للحذف» كانا
> فخّين حقيقيين أمسكهما الفحص قبل التنفيذ.

---

## الفخّان اللذان كادا يُوقعان

### الفخ الأول: دوال تبدو ميتة، وعملاء أحياء يستدعونها

عميل Flutter يستدعي **دالتين فقط**: `apply_changes` و`pull_changes`.
بحثٌ نصّي في `lib/` كامل عن أسماء الدوال الأخرى ومعاملاتها أعطى **صفر
إشارة**. الاستنتاج المتسرّع: بقية الدوال ميتة، فلتُحذف.

لكن `pg_stat_statements` قال غير ذلك — استدعاءات **حقيقية عبر PostgREST**:

| الدالة | استدعاءات فعلية |
|---|---|
| `search_patients_by_phone` | 842 |
| `fetch_patient_history` | 506 |
| `search_patient_by_name` | 442 |
| `search_debts_by_name` | 3 |

**المستدعي عملاء Vue/Capacitor القدامى ما زالوا منصَّبين ويعملون في
العيادات.** حذف هذه الدوال كان سيكسر عندهم شاشة سجل المريض والبحث
بالاسم والبحث بالهاتف — بلا أي أثر في مستودع Flutter يدلّ على السبب.

> **قاعدة مستخلَصة:** في هذا المشروع، غياب الاستدعاء من شيفرة Flutter
> **لا يعني** أن الدالة ميتة. التوزيع بالتنصيب الجانبي يعني أن نسخاً
> قديمة تبقى حيّة إلى أجل غير مسمّى. المرجع الوحيد المقبول قبل حذف أي
> دالة خادمية هو `pg_stat_statements`، لا `grep`.

### الفخ الثاني: جدول مكرر 100٪… إلا في ثلاثين صفاً

`sync_rows_legacy` بقية من مهاجرة التقسيم، وتعليق المهاجرة نفسه يقول
حرفياً *«بعد التحقق من تطابق الأعداد، أسقط الجدول القديم يدوياً»*.
و146 صفاً كلها موجودة بمعرّفاتها في `sync_rows`.

لو اكتفيتُ بمطابقة المعرّفات لأوصيتُ بالإسقاط فوراً. لكن مقارنة
**المحتوى** كشفت أن **30 صفاً من 146 محتواها مختلف**.

فأيّ الجانبين أحدث؟ حكّمتُ مقارنَين مستقلين، أحدهما دالة المحرك نفسها:

| المقارن | `sync_rows` أحدث | `legacy` أحدث | ملتبس |
|---|---|---|---|
| `hlc_is_newer` (مقارن المحرك) | **30** | 0 | 0 |
| `updated_at` | **30** | 0 | — |

إجماع تام: الجدول القديم **بائت بالكامل**، ولا صف فيه يحمل جديداً.
آمن للإسقاط — لكنه بيانات، فينتظر إذنك (أدناه).

---

## ما نُفِّذ فعلاً

### ١. إسقاط `idx_sync_rows_entity_date` (مهاجرة 0018)

64 فهرساً مادياً، 1664 كيلوبايت. الدليل الحاسم لم يكن عدّاد المسحات
(4 في 82 يوماً) بل **المخطِّط نفسه**: `EXPLAIN ANALYZE` للاستعلام الذي
بُني له حرفياً، والفهرس موجود:

```
Parallel Seq Scan on sync_rows_p61     ← تجاهله تماماً
Execution Time: 65.305 ms
```

وبإطفاء كل الفهارس قسراً: **52.8 مللي — أسرع لا أبطأ**. الفهرس كان
يُدفع ثمنه على كل إدراج `records`/`prosthetics` ولا يُستعمل.

التراجع أمر واحد، مكتوب حرفياً في رأس المهاجرة 0018.

### ٢. استرداد انتفاخ `user_data`

**496 كيلوبايت لجدول فيه صفر صف.** الجدول حيّ ويُقرأ 6,217 مرة — فلا
يُسقط — لكن مساحته كانت هدراً صافياً. `VACUUM FULL` أعادها إلى **24 ك.ب**.

### الحصيلة

| | قبل | بعد |
|---|---|---|
| حجم القاعدة | 107 م.ب | **105 م.ب** |
| `user_data` | 496 ك.ب | **24 ك.ب** |
| فهارس منطقية | 13 | 12 |
| صفوف `sync_rows` | 149,892 | **149,892** (لم تُمسّ) |

تحقق ما بعد التنفيذ: العزل سليم (149,006 / 0 / 0)، و`pull_changes`
وكل دوال العملاء القدامى تعمل، و`fetch_entity_month` تعيد نتائجها
كاملةً بلا الفهرس المحذوف.

---

## ما فُحص فلم يكن قابلاً للحذف

| المرشَّح | الحكم | السبب |
|---|---|---|
| فهارس مكررة | **لا يوجد** | الـ13 كلها تعريفات متمايزة. الـ180 تحذيراً كانت الفهرس الواحد × 64 شريحة |
| `user_data` (فارغ) | **يبقى** | 6,217 قراءة — حيّ، فارغ فقط |
| `applied_ops` | **يبقى** | 17,335 قراءة، و`pg_cron` يقلّمه يومياً 3:15 |
| 433 شاهد قبر | **تبقى** | لازمة لانتشار الحذف عبر المزامنة، و0.3٪ من الحجم |
| فهارس `sync_rows_legacy` الثلاثة | **تبقى** | 72 ك.ب مقابل خطر — لا يستحق |
| كل الدوال ذات الاستدعاءات | **تبقى** | الفخ الأول أعلاه |

---

## تنظيف ما قبل الإطلاق — نُفِّذ (2026-07-30)

بعد أن أفاد المالك أن التطبيق **قبل الإطلاق ولا بيانات حقيقية فيه**،
تأكّد ذلك بالفحص: الحساب الأكبر يحمل 149,006 صفاً **كلها من يوم واحد
(2026-06-22)** — 144,004 سجلاً و5,000 مريض، بصفر موعد وصفر أشعة. بذرة
حِمل مولَّدة لا استعمال عيادة.

**النطاق المطلوب:** مسح كل بيانات المزامنة عدا الحسابين النشطين.

### الحرّاس قبل التنفيذ

معيار الإبقاء المبني على **عدد الصفوف** كان فخّاً: أحد الحسابين كتب في
اليوم نفسه، ولو كتب صفاً آخر لتغيّر عدده من 325 ولحُذف. فثُبِّت المعيار
على `max(updated_at) >= 2026-07-29`، وأُضيف حارسان داخل المعاملة يُلغيان
العملية كلها بـ`RAISE EXCEPTION` إن لم يكن عدد المستبقين 2 بالضبط، أو
خرج عدد صفوفهم عن نطاق 400–600.

وحُسبت **بصمة تحقّق** للصفوف المستبقاة قبل الحذف:
`md5` على `user_id:entity:id:_hlc:md5(payload)` لكل صف.

### النتيجة

| البند | قبل | بعد |
|---|---|---|
| حجم القاعدة | 105 م.ب | **24 م.ب** (−77٪) |
| `sync_rows` (64 شريحة) | 88 م.ب | 8,592 ك.ب |
| `applied_ops` | 2,840 ك.ب | 1,288 ك.ب |
| `sync_rows_legacy` | 408 ك.ب | **مُسقَط** |
| صفوف `sync_rows` | 149,892 | **433** |
| صفوف `applied_ops` | 6,700 | 4,408 |
| صفوف يتيمة | 406 | **0** |
| `applied_ops` يتيمة | 2,292 | **0** |
| حسابات لها بيانات | 24 | 2 |
| `auth.users` | 24 | **24** (لم تُمسّ) |

**بصمة الصفوف المستبقاة قبل الحذف وبعده: `bea1cf400d3157423a8331085738481e`
— متطابقة حرفياً.** الحسابان سليمان بت ببت.

### التحقق الوظيفي بعد التنفيذ

```
حساب A: 108 صفاً ✓   ·   يرى صفوف B: 0 ✓
حساب B: 325 صفاً ✓   ·   anon: 0 ✓
pull_changes · fetch_patient_history · search_patients_by_phone : تعمل ✓
get_monthly_summary : تعمل ✓  ← عادت للعمل، انظر أدناه
```

### أثر جانبي مُرحَّب به

`get_monthly_summary` كانت **معطّلة تماماً** بخطأ `invalid input syntax
for type integer: "false"`. الصفوف الـ54 المسبّبة كانت كلها في حسابات
شملها التنظيف، فعادت الدالة للعمل.

> **لكن هذا ستر للعرَض لا علاج للسبب.** إن كان أي عميل ما زال يكتب
> `isDebt` منطقياً فستعود المشكلة مع أول صف حقيقي. الشاهد المطمئن:
> 265 سجلاً كُتبت بعد 22 يونيو **كلها رقمية بلا استثناء** — أي أن
> العميل الحالي يكتب النوع الصحيح، وكانت حادثة جيل سابق.

### ملاحظة سعة: أرضية التقسيم الثابتة

بعد التنظيف، **62 شريحة من 64 فارغة تماماً** (حسابان فقط، موزَّعان
بالتجزئة) وتحتلّ **2,480 كيلوبايت** من البنية وحدها. مع جداول TOAST
لكل شريحة يبلغ مجموع الأرضية الثابتة نحو 8 م.ب بلا أي بيانات.

على ميزانية 500 م.ب هذا 1.6٪ — مقبول. لكن اعرفه عند قراءة أرقام
الاستهلاك في الأشهر الأولى: **أول ~8 م.ب ليست بياناتك**.

### ما لم يُحذف عمداً

- **`auth.users`** — لم يُطلَب حذف الحسابات، فبقيت الـ24 كاملة. لا يزال
  22 حساباً بلا بيانات؛ إن أردت تصفيرها فهي خطوة منفصلة.
- **4,408 صفاً في `applied_ops`** تخص حسابات قائمة، منها ما يخص حسابات
  حُذفت بياناتها. لا ضرر منها: `prune_applied_ops(30)` يعمل يومياً 3:15
  عبر `pg_cron` وسيصفّيها تلقائياً خلال شهر.
- **179 شاهد قبر** ضمن الـ433 المستبقاة — لازمة لانتشار الحذف عبر
  المزامنة، ولا تُحذف.

---

## عطل قائم كشفه الفحص (خارج نطاق التنظيف)

`get_monthly_summary` **معطّلة تماماً على البيانات الحالية**:

```
ERROR: invalid input syntax for type integer: "false"
```

السبب: الدالة تكتب `(payload->>'isDebt')::int`، و**54 صفاً من 144,321**
تحمل `isDebt` قيمةً منطقية `false` بدل رقم. وصفٌّ واحد فاسد يُسقط
التقرير الشهري كلّه لا سطراً منه.

هذا **تضارب أنواع بين أجيال العملاء** — أحدهما يكتب منطقياً والآخر
رقمياً. والحقول المصابة ثلاثة: `isDebt` (54)، `isPros` (54)،
`isDebtPayment` (25).

العلاج (لم أطبّقه — إصلاح منطق لا تنظيف):

```sql
-- بدل (payload->>'isDebt')::int
coalesce(
  case jsonb_typeof(payload->'isDebt')
    when 'boolean' then (payload->'isDebt')::boolean::int
    when 'number'  then (payload->>'isDebt')::int
    else 0
  end, 0)
```

> الأهم من العلاج: **الشيفرة يجب أن تكتب نوعاً واحداً**. أي إصلاح على
> الخادم وحده يعالج العرَض ويترك المصدر ينزف صفوفاً جديدة مختلطة الأنواع.
