# آخر التعديلات — م146: اللوح الجانبي «عقد التصميم د» لنموذج الزيارة المكتبي (2026‑08‑08)

## م146 — إعادة تصميم كاملة لنموذج «زيارة جديدة/تعديل» على سطح المكتب

**الشكوى (المالك، مع لقطة):** اللوح السفلي (عقد «ج») يتشوه عند التوسعات —
تركيبات/دين تقفزان بالتخطيط وترفعان اللوح بين ارتفاعين؛ الحاسبة زر ضخم
بعيد عن حقل القيمة؛ toggles متناثرة؛ أعمدة متفاوتة. طلب حرفي: تصميم جديد
كلياً مناسب للكمبيوتر بلا تمرير، بدراسة أنماط، وبلا فقدان أي وظيفة —
واللوح المنزلق من اليمين أنسب.

**عقد «د» — الجوهر:**
- **لوح جانبي عائم** ينزلق من اليمين (تحت الشريط الجانبي RTL) فوق مساحة
  العمل بظل بلا حاجز معتم (اختيار المالك) — الجدول يبقى مرئياً، عرض ثابت
  560-660، ارتفاع كامل، وإطار لا يتغير أبداً: التوسعات تتحرك داخله
  بـAnimatedSize ناعمة (نقيض قفزات عقد «ج» — يحرسه اختبار ثبات الإطار).
- **كثافة مكتبية:** المسميات تعوم داخل إطارات الحقول (ddec) بدل سطر مستقل
  فوق كل حقل — جوهر ميزانية اللاتمرير: أسوأ حالة (تركيبات+دين+تحاليل معاً)
  تسع شاشة 1366×768 كاملة بلا تمرير، يحرسها اختبار دائم في CI + لقطات
  ذهبية تولد محلياً (GOLDENS=1 مع تحميل خطوط التطبيق الحقيقية).
- **الدفع شريط مقسم واحد** كاش|تحويل|دين (بديل قائمة منسدلة + مفتاح دين
  منفصل) بالنمط المكتبي القياسي للاختيارات المتنافية الفورية؛ اختيار دين
  يكشف الدفعة الأولى وطريقتها (كاش|تحويل مصغر) — نفس نموذج البيانات حرفياً
  (payment + isDebt)، ولا فقدان لتركيبة «دين بدفعة أولى تحويلاً».
- **الحاسبة suffix صغير داخل حقل القيمة** (طلب المالك حرفياً).
- **رقاقات ميزة بدل toggles** لتحديد الأسنان والمعلومات الطبية (الفعل «افتح
  محرراً» لا «إعداد ثنائي») — بحالة ظاهرة (عداد الأسنان/علامة التعبئة)
  وزر مسح صغير، مدمجة في سطر عنوان القسم.
- تركيبات: صفا إدخال كثيفان + سطر معاينة واحد (كان ثلاثة)؛ التحاليل
  الثلاثية: الصح وقائمة دفعها في صف واحد؛ الملاحظات والحفظ صف ختامي واحد؛
  اقتراحات الأسماء بسقف ارتفاع تتمرر داخلياً.
- **م146/ملحق:** بطاقة «دخل اليوم» كانت تفيض بكسلات مع مقاييس خطوط مختلفة
  (كشفتها اللقطات) — لفت بـFittedBox ينكمش عند الضيق فقط.

**بلا مساس:** نسخة الهاتف حرفياً؛ كل مفاتيح rec-* القائمة (المستبدلة فقط:
rec-payment/rec-debt/rec-report-tgl/rec-medical-tgl صارت rec-payment-seg/
rec-debtpay-seg/rec-report-open/rec-medical-open في اللوح المكتبي وحده)؛
المنطق والحفظ (record_saver) والمزامنة.

**الاختبارات:** ملف اللوح أعيدت كتابته لعقد «د» (7 اختبارات: الفتح
والإغلاق، المفاتيح، الحاسبة داخل الحقل، ثبات الإطار مع الدين، الطفو فوق
المساحة) + ملف لقطات ذهبية بحارس لا-تمرير دائم. **الحزمة الكاملة 1143
اختباراً خضراء محلياً** — أول تشغيل محلي كامل بعد ترجمة SQLCipher لبيئة
العمل (كانت الاختبارات القاعدية أسيرة CI).

---

# آخر التعديلات — م145: «التحاليل الثلاثية» — مطابقة مواصفة المالك (2026‑08‑08)

## م145 (تسليم الوكيل الجديد) — إعادة تشكيل نظام التحاليل على المواصفة الحرفية

**السياق.** نظام «التحاليل» السابق بُني أعمّ من مواصفة المالك (قائمة تحاليل
متعددة بأسماء وأسعار حرّة)، والمالك قرّر الالتزام الحرفي: تحليلٌ واحد ثابت
«التحاليل الثلاثية» بسعرٍ افتراضي من الإعدادات وزرِّ تفعيلٍ واحد. التدقيق
كشف أيضاً أخطاء حقيقية: واجهة التحليل كانت تظهر في وضع تعديل الزيارة
وقيمها تُتجاهل صامتاً، ومنطقة التحليل تظهر حتى بقائمة تحاليل فارغة.

**ما تغيّر:**
- **الإعدادات** (`analyses3.dart` جديد + `settings_screen.dart`): قسم
  «التحاليل الثلاثية» الواحد — سعر افتراضي (tri-anal-price) وزر تفعيل
  (tri-anal-enabled) يتحكم بظهور الخيار في نموذجَي الإضافة. التخزين خريطة
  {price, enabled} تحت `config['analyses3']` عبر مسار الكتابة المتزامن
  المعتاد. حُذفت آلية القائمة القديمة كاملةً (analysesList/enabledAnalyses
  وبطاقتها وعتادها).
- **نموذجا الإضافة** (`add_record_screen.dart`، `quick_visit_sheet.dart`):
  مربع صحّ «التحاليل الثلاثية» يظهر فقط حين الميزة مفعّلة **وفي الإنشاء
  حصراً** (إصلاح خطأ الظهور الصامت في التعديل)؛ تفعيله يُظهر طريقة الدفع
  كاش/تحويل فقط — بلا حقل سعر (السعر ثابت من الإعدادات). البوابة موحّدة
  في دالة نقية `triAnalysisFor` (record_saver) تعيد null ما لم يتحقق:
  إنشاء + مؤشَّر + مفعّل + سعر > 0.
- **جدول إدخال اليوم:** الكمبيوتر — الخلية تبقى فارغة بلا تحليل (كانت
  شرطة)، وبند «حذف التحاليل» في قائمة الصف خلف records.delete بتأكيد؛
  الهاتف — أزرار حذفٍ لكل بندٍ في نافذة «التحاليل ✓» خلف الصلاحية نفسها.
  أُضيف حقل `id` اختياري لـ AnalysisLink (افتراضيه '' للتوافق الخلفي)
  يمكّن الحذف المباشر عبر repos.records.delete (حذف ناعم متزامن).
- **بطاقة التحاليل في الخزينة** (المنصتان): مربع بحث أعلى القسم + زر
  فلترة (جميع التحاليل/كاش/تحويل) بدالة نقية `filterAnalysesRows`
  (analyses_filter.dart) بتطبيع عربي (arNorm) على اسم المريض واسم
  التحليل. الفلتر عرضيّ — المجاميع والطباعة على كامل البنود.
- **لا مساس** بالعزل المالي الرباعي (صف isAnalysis عبر AnalysisInput)
  ولا بمسار الحفظ ولا بعمود الكمبيوتر الملوّن (أخضر كاش / أصفر غامق
  0xFF8A6D1B تحويلاً — مطابق للمواصفة أصلاً).

**الاختبارات:** 65 اختباراً نقياً جديداً في أربعة ملفات (tri_*_test)،
وأُعيدت كتابة مجموعة إعدادات analyses_test للنموذج الجديد. محلياً: analyze
صفر، ولا انحدار في المجموعة القابلة للتشغيل محلياً (501 نجاح والإخفاقات
البيئية الـ640 ذاتها — بيئة العمل بلا SQLCipher والحزمة الكاملة تُفرض في
CI). نُفّذ البناء بأربعة وكلاء متوازيين بملكية ملفات صارمة تحت إشراف
الوكيل المشرف الذي راجع المفاصل وأغلق الثغرات.

---

# آخر التعديلات — م135: تفعيل الاشتراك وحارس المزامنة (المرحلة ٥) (2026‑08‑05)

## م135 (البناء 135) — المرحلة ٥: «الأسنان الحقيقية»

**الغاية.** حتى الآن كان النظام واجهةً: اللوحة تدير اشتراكات لا تمنع شيئاً.
هذه المرحلة تُدخل المنع الفعلي — شاشة تفعيلٍ في العيادة، وحارسٌ خادميٌّ
يرفض مزامنة الكتابة لاشتراكٍ غير فعّال.

**جهة الخادم (النظام المعتمد — هجرة 0053):**
- `license_allows_sync(uid)`: يعيد true دائماً بينما `gate.enforce=false`
  (الوضع الحالي)، وإلا true فقط لاشتراكٍ active/trial غير منتهٍ.
- `apply_changes` (مسار كتابة المزامنة) أُعيد إنشاؤه **مطابقاً حرفياً** مع
  إضافة سطر حارسٍ واحد بعد فحص المصادقة. أُثبت سلوكياً على الإنتاج: بدفعة
  حقيقية والإجبار مطفأ ⇒ تُطبَّق كما كانت (متطابق)، وبمحاكاة الإجبار مفعّلاً
  ⇒ يُحجب اشتراكٌ غير فعّال — والاختبار تراجع ذاتياً فبقي الإجبار مطفأً.
- السحب (`pull_changes`) يبقى مفتوحاً عمداً: بياناتٌ للمالك وحده، والمنع في
  الكتابة لا القراءة.

**جهة العيادة:**
- `data/sync/transport.dart`: واجهة `LicenseTransport` (verify_license +
  activate_code)، ينفّذها `SupabaseTransport`؛ الوضع المحلي بلا تنفيذٍ لها.
- `features/auth/license_service.dart`: الحسم الكامل — الإجبار، الحالة،
  الانتهاء بالتاريخ، **فترة سماحٍ دون اتصال** (grace_days، افتراضي ٧)،
  و**دفاع تلاعب الساعة** (نخزّن آخر وقت خادمٍ موقَّت؛ إرجاع ساعة الجهاز
  خلفه = حجب). بلا سحابة تُسمح كل الأحكام (سلوك الاختبارات والوضع المحلي).
- `PostLoginGate`: حالةٌ سادسة **activation قبل setup** — تُعرض حين يمنع
  الترخيصُ الدخول برسالةٍ حسب السبب (منتهٍ/محظور/مجمَّد/بلا اتصال/تلاعب
  ساعة) + حقل كود + تفعيل + خروج. وإعادة التحقق عند **العودة من الخلفية**
  داخل الصدفة، فحظرٌ/تجميدٌ من اللوحة يُغلق الوصول عند التحقق التالي.

**بينما `gate.enforce=false`:** كل هذا خامدٌ تماماً — البوابة تُسمح دائماً،
والحارس الخادمي يُسمح دائماً. تفعيلُ الإجبار قرارُ المالك بضغطة (تبديل علم
`gate` في `app_private.config`) بعد أن يوزّع أول الأكواد.

**الاختبارات:** `m135_license_gate_test` (١٢ اختباراً: الإجبار، الحالات،
الانتهاء، السماح دون اتصال، تلاعب الساعة، التفعيل) — والحزمة كاملةً خضراء.
النسخة: v1.0.135.

---

# سجلّ م134: حصة تخزين صور الأشعة (المرحلة ٣) (2026‑08‑05)

## م134 (البناء 134) — المرحلة ٣ من مشروع التراخيص: التخزين

**السياق.** المرحلة ٣ من نظام التراخيص تضيف حصة تخزينٍ لصور الأشعة: اللوحة
تعرض الاستهلاك والنمو والتوقّع لكل مستخدم، وتطبيق العيادة يعرض شريط حصته
ويمنع الرفع عند الامتلاء. هذا الملف يوثّق تعديلات **تطبيق العيادة** وحدها؛
اللوحة مشروعٌ مستقل (control_panel).

**الحقيقة المعمارية.** أحجام الصور تعيش في Cloudflare R2 لا في القاعدة،
والتطبيق **يحذف النسخة الكاملة محلياً بعد الرفع** (يبقي المصغّرة) — فمسح
الملفات المحلية يكذب. لذا المحاسبة **عدّادٌ بخريطة {مفتاح→بايتات}** في
`metadata` (device-local): مجموع القيم = الاستهلاك، فالحذف نقصٌ دقيق بلا
انجراف، ويعمل بلا اتصال.

**ما أُضيف:**
- `features/xrays/storage_meter.dart`: المقياس — خريطة الأحجام، العتبات
  (80/90/100)، `check()` للحجب، `refreshQuota`/`reportUp` عبر قناة
  `StorageTransport` الجديدة (get_my_storage/report_my_storage على الخادم).
- `data/sync/transport.dart`: واجهة `StorageTransport` منفصلة — النقل
  المزيّف (الوضع المحلي) وخوادمُ أقدمُ سليمةٌ بلا تنفيذها؛ ينفّذها
  `SupabaseTransport` وحده.
- **خط أنابيب الرفع** (`xray_pipeline.dart`): حارسٌ قبل الرفع (صورة تتجاوز
  الحصة تبقى معلّقةً بلا رفع — لا نتخطّى حدّ R2 ولو امتلأ الطابور دون
  اتصال) + محاسبةٌ عند نجاح الرفع بالبايتات الفعلية والمفتاح النهائي (بعد
  أي إعادة تسمية) + تبليغ الخادم أفضل جهد.
- **الرفع اليدوي** (`xray_section.dart`): فحصٌ مسبق يمنع الإدخال عند
  الامتلاء بحوار «التخزين ممتلئ — احذف صوراً قديمة أو رقِّ الاشتراك»؛
  والحذف (مفرداً وجماعياً) يحرّر مساحة الحصة فيعود الرفع تلقائياً.
- **الإعدادات** (`settings_screen.dart`): بطاقة «حصة تخزين صور الأشعة» في
  قسم التخزين (الوضع السحابي) — شريطٌ ملوّن بالعتبات + المتبقّي + رسالة
  حالة صريحة، تتحدّث حيّاً مع كل رفع/حذف.

**على الخادم (النظام المعتمد):** هجرة `0051` أضافت `get_my_storage`
و`report_my_storage` و`storage_usage_daily` و`admin_storage_page` — موثّقة
في `supabase/ADOPTED_SYSTEM_AR.md`.

**الاختبارات:** `m134_storage_quota_test` (خريطة الأحجام، العتبات، الحجب،
الحصة والتبليغ، الفشل الآمن) — والحزمة كاملةً خضراء بلا مساس. النسخة:
v1.0.134.

---

# سجلّ م133: استعادة خط الأساس الأخضر + خمسة إصلاحات كشفها (2026‑08‑05)

## م133 (البناء 133) — المرحلة صفر من مشروع التراخيص

> ملاحظة توثيقية: هذا السجل كان متوقفاً عند م109 بينما الشيفرة وصلت م132
> (نظام الموظفين م118–م120، شاشة «دخل اليوم»، دمج الدفعات م130–م132…) —
> فجوةٌ ورثتها هذه النسخة ولم تُردَم هنا رجعياً؛ المعالم مُوثَّقة في
> تعليقات الشيفرة واختباراتها.

**الواقعة.** أرشيف v132 وصل بحزمة اختبارات **حمراء أصلاً**: ‏124 فشلاً من
914. لا عطلَ منتجٍ وراء أغلبها — بل اختبارات تقادمت عن إعادة هيكلة
م110–م132 ولم تُحدَّث معها: بوابة دخول الموظفين (م118) صارت تسبق كل شيء،
و«الرئيسية» صارت شاشة دخل اليوم بينما نموذج الإدخال القديم صار ورقةً
سفلية تُفتح بالزر العائم.

**الإصلاح — جهة الاختبارات (لا مساس بسلوك المنتج):**
- `test/staff_test_session.dart` جديد: جلسة إدارةٍ اختبارية تُحقن في
  `overrides` فيقلع التطبيق داخل الصدفة — التوأم الاختباري لدخول المالك،
  والبوابة نفسها تبقى مُختبَرة في ملفات نظام الموظفين. المرآة العامة
  `kCurrentStaff` لا تُمسّ عمداً (قاعدة «null ⇒ اسمح» مسار الاختبارات
  الشرعي، و`staffCreatedBy()` يبقى لا يكتب حقلاً لم يكن يكتبه).
- ترميم 19 ملف اختبارٍ متقادماً: تكييف **مسارات الوصول** مع البنية
  الجديدة (فتح النموذج بـ`fab-add`، خطوة `fin-seg-*` لقائمة بطاقات
  المالية م108، `pumpAndSettle` لانتقالات الصفحات، حذف توقع شارة دينٍ
  أزالها المالك نهائياً في م125، مواءمة m98 مع بديل الكشف الوظيفي
  `StatementSection` بعدما مات مساره القديم شيفرةً خاملة) — الجوهر
  المُختبَر نفسه بلا تليين.

**الإصلاح — جهة المنتج (خمسة أعطال حقيقية كشفتها الاختبارات المستعادة):**
1. **ورقة «موعد متابعة» تخطف النقرات** (`add_record_screen.dart`): زر
   الموعد كان يبدّل التبويب للتقويم **ولا يغلق ورقته** — فتبقى فوقه
   شفافةً تبتلع كل نقرة. صار يغلق عبر `onSaved` كمسار الحفظ تماماً
   (وخارج الورقة لا أثر — الحارس نفسه).
2. **فيضان شريط فلاتر «دخل اليوم»** (`daily_income_screen.dart`): أربع
   رقاقات وSpacer في صفٍّ صلب — فيضان 347ن بأقصى مقياس خط ويلامس الحافة
   حتى على 393dp. صار تمريراً أفقياً **عند الضيق فقط**: قيدُ عرضٍ أدنى +
   `spaceBetween` يطابقان توزيع Spacer الأصلي حرفياً عند السعة.
3. **فيضان عنوان بطاقة الدخل** (نفس الملف): «دخل اليوم • التاريخ» نصٌّ
   صلب يفيض 251ن بالخط الأكبر — حُمي بنمط م95 المطبَّق على خانات البطاقة
   نفسها (انكماشٌ بسطر واحد بدل الفيضان).
4. **فيضان بطاقة السجل في ملف المريض** (`patient_profile_screen.dart`):
   عمود المبلغ/التاريخ كان `FittedBox` عارياً بلا `Flexible` — فحين
   تمتلئ كل الأعمدة (أسنان + شارة + دفعة + طريقة دفع) يدفع الصف فوق
   سعته ويومض فيضان 35ن أثناء انزلاق الانتقال. صار يتنازل كأشقّائه —
   مبدأ v48 «لا فيضان أبداً» مُستعاد.
5. **مقام شاشة الإثبات المتيبّس** (`db_proof.dart` + `proof_screen.dart`):
   التسمية كانت تكتب «/13» نصاً بينما المخطط نما إلى 15 جدولاً
   (employees/expenses) فتعرض «15/13». صار المقام يُقرأ من
   `expectedTables.length` نفسها فلا يكذب مع أي نموٍّ لاحق.

**وإصلاح بوابةٍ موروث:** استيراد غير مستعمل في `m97_lock_defaults_test`
كان يُسقط `flutter analyze --fatal-warnings` — حاجز CI نفسه.

**الاختبارات: 914/914 خضراء · المحلل: 0 أخطاء 0 تحذيرات.**
النسخة: v1.0.133 (أول بناء من بيئة البناء الجديدة المعزولة).

---

# سجلّ م109: صقل الورقة والكشف والشارة (2026‑08‑02)

## م109 (البناء 109) — أربعة طلبات

1. **ورقة ملف المريض**: حُذف صف «عدد الزيارات/العملة» من جدول النموذج
   (المعلومة باقية في النظام؛ العملة ظاهرة في شريط المجاميع).
2. **إعادة تسمية من المصدر**: «الأمراض المزمنة والحساسية» في حوار
   المعلومات الطبية صارت **«الأمراض العامة»** (موضعان)، وتسمية
   «الحالات» في الورقة صارت «الأمراض العامة» — مفتاح التخزين
   `conditions` لم يُمس (توافق تام).
3. **الكشف المالي أبسط**: رأسٌ بصفٍّ واحد بدل أربعة — بحث + **زر مدى
   واحد** (منتقي المدى الأصيل بدل زرّي من/إلى) + **زر فلاتر** يفتح
   ورقة سفلية برقائق العيادة والفئة مع شارة عدّ المفعل وزر «مسح
   الكل» + طباعة. كل الوظائف والمنطق كما هي (`m99` مُحدَّث).
4. **شارة الدين الحمراء**: توسيط الرقم في قلب الدائرة تماماً —
   `textHeightBehavior` يلغي انحياز خط القاهرة + أرقام جدولية +
   22 نقطة، مع اختبار هندسي يقيس انطباق المركزين (≤0.6ن) —
   `m109_ui_polish_test`.

---

# سجلّ م108: التناظر المحاسبي الكامل (2026‑08‑02)

## م108 (البناء 108) — ملاحظات المالك الخمس على تصميم 107

1. **دمج الدفعات في جدول معالجتها**: `baseServiceOf` تجرّد بادئتي
   «دفعة دين — X» و«دفعة تركيبات — X»، والتسميات المجردة (دفعة أولى…)
   تقرأ خدمة الأساس من لقطة النسب (treatmentId) — فتهبط كل دفعة صفاً
   داخل جدول معالجتها والأرقام تتجمع نفسها بلا أي تغيير.
2. **تناظر الأعمدة عبر كل الجداول**: شبكة أعمدة مثبتة واحدة (التاريخ 58،
   الاسم مرن، الإيراد/طبيب/عيادة 64، الدفع 48) لكل جداول الكشف — الإيراد
   تحت الإيراد وطبيب تحت طبيب طول الورقة، ومجموعات 0% بنفس الأعمدة
   بشرطات، ورؤوس موحدة (النِّسَب في عنوان المجموعة).
3. **الإجمالي النهائي صف واحد بنفس الشبكة**: الإيرادات وربح الطبيب وربح
   العيادة كلٌّ تحت عموده بخط علوي أسود — بلا قسم مستقل.
4. **عناوين المجموعات وسط الصفحة** (معامل توسيط في عنوان القسم).
5. **ملف المريض**: جدول نموذج متناظر واحد (تسمية رمادية 64 نقطة | قيمة)
   للبيانات والطبية معاً، والحقول الطويلة بنفس عرض عمود التسمية.
6. **تراصف شبكات الأسنان**: نصفا الصليب بعرضين ثابتين (34+34) في PDF —
   الخط الوسطي بنفس الموضع في كل صف فتتراصف الشبكات عمودياً.

**الاختبارات**: `m108_statement_merge_test` (تجريد البادئات، اللقطة،
الدمج بأرقام متطابقة، الترتيب الزمني) + m38 القديم أخضر بلا مساس +
المعاينة الصورية اعتُمدت. الحزمة كاملة خضراء.

---

# سجلّ م107: نظام الطباعة المحاسبي الأحادي (2026‑08‑02)

## م107 (البناء 107) — إعادة تصميم الطباعة كلياً: أبيض/أسود احترافي

**اعتراضا المالك (بلقطات)**: الأشرطة الداكنة (عناوين الأقسام ورؤوس
الجداول) والصفوف الزائدة (بيانات بسطرين، تاريخ مكرر، صناديق مجاميع
ضخمة). **القرار**: نظام مستند محاسبي أحادي في كل التقارير.

**النظام الجديد (المساعدات المشتركة — يرثه كل تقرير)**:
- صفر تعبئة داكنة: عناوين الأقسام نص أسود عريض فوق خط فاصل رفيع.
- رؤوس الجداول: رمادي فاتح جداً (F2F2F2) بنص أسود بين خطين أسودين
  (نمط دفتر الأستاذ)، زيبرا F7F7F7، حدود شعرية D9D9D9، صف المجموع
  فاتح بخط علوي أسود 1.2، وصندوق التنبيه أحادي أيضاً.
- هوامش وحشوات مضغوطة (32←24 و22←18، خلايا 4/2.5).

**ملف المريض — ضغط بنيوي بنفس المعلومات**:
- ترويسة سطر واحد: شعار صغير + اسم المركز + «ملف مريض • التاريخ»
  (كان التاريخ مكرراً مرتين) ثم خط أسود.
- بيانات المريض: سطر واحد بأربع خلايا ذاتية التسمية (بلا شريط عنوان).
- المعلومات الطبية: سطر نصي واحد مظلل فاتحاً.
- المجاميع: شريط ليدجر واحد بدل ثلاثة صناديق. تواقيع وتذييل أضيق.

**المعاينة**: وُلّدت صورياً (pdftoppm) واعتُمدت قبل البناء. أداة
المعاينة `m107_preview_tool_test` باقية للتكرار التصميمي مستقبلاً.

---

# سجلّ م106: ضبط مقاسات الشبكة (2026‑08‑02)

## م106 (البناء 106) — إصلاحا مقاس بعد تجربة المالك لـ105

1. **الطباعة**: صفوف الجدول تضخمت عشوائياً — السبب `pw.FittedBox` حول
   الشبكة في خلية الجدول (يتمدد لملء عرض العمود فيتضخم الارتفاع بحسب
   المحتوى). أزيل، والشبكة الآن بحجمها الطبيعي المضغوط وسط الخلية
   (خط 7، حشو رأسي 1، سماكة 0.8) — صفوف موحدة كجدول احترافي.
2. **البطاقة**: الشبكة كانت صغيرة جداً (مقياس 0.8) — رُفعت إلى 1.15
   والانكماشُ الذاتي يحمي من تجاوز عرض العمود، فتكبر بكل ما يتسع له.

---

# سجلّ م105: شبكة Palmer الصليبية عند تعدد الأرباع (2026‑08‑02)

## م105 (البناء 105) — الشبكة الصليبية الكلاسيكية في البطاقة والطباعة

**طلب المالك (بصور مرجعية)**: عند تعدد الأسنان تُعرض شبكة Palmer
الكلاسيكية: `E D C B A | A B C D E` حول الخط الأفقي، وعند فكٍّ واحد
نصفُها فقط بلا امتداد العمودي للنصف الغائب.

**القاعدة المعتمدة ثلاثية المستويات**:
1. ربع واحد ⇒ خلية القوس بالمدى (م104) — رمز الربع الصحيح.
2. فك واحد متعدد الأرباع ⇒ نصف الشبكة (خط أفقي + وسطي يخترق سطره فقط).
3. الفكان ⇒ الصليب الكامل.

**الاتجاه السريري الأصيل**: قسم يمين المريض يُكتب من الوحشي نحو الخط
المتوسط (`8-1` / `E-A`) وقسم اليسار بالعكس (`1-8` / `A-E`) — مطابقةً
للصور. FDI بأرقامه الكاملة في نفس الشبكة (`18-11 | 21-28`).

**التنفيذ**: `ToothCrossModel` في وحدة التلخيص (نقي، null لربعٍ واحد) +
`ToothCrossView` (فلاتر) و`_pwToothCross` (PDF) — كلاهما **جدول بحدود
داخلية** (`verticalInside`/`horizontalInside`): الهندسة الصليبية تُرسم
حدوداً لا أعمدةً ممددة، فلا فيضان ولا تمدد لانهائي، مع انكماش ذاتي داخل
عمود البطاقة/الجدول.

**الاختبارات**: 7 وحدة للنموذج (الاتجاه المرآتي، النصفان، FDI، اللبني
كصورة المالك، المختلط) + واجهة الفم الكامل (صليب Palmer وFDI) وتحديث
حراس م104/م11. الحزمة كاملة خضراء. عرض محض — صفر تغيير تخزين.

---

# سجلّ م104: خلايا Palmer حقيقية + التجميع الربعي بالمدى (2026‑08‑02)

## م104 (البناء 104) — عرض أسنان البطاقة بالرمز الصحيح وحل الأعداد الكبيرة

**طلب المالك**: رقم السن يظهر برمز Palmer الحقيقي (خطا الربع) في بطاقة
السجل، مع حل مرتّب للأعداد الكبيرة (فك كامل/فم كامل) يظهر في الطباعة أيضاً.

**التصميم — التجميع الربعي بالمدى (العرف السريري)**:
- وحدة نقية جديدة `tooth_summary.dart`: تجميع حسب (الربع، الطقم)،
  ترتيب من الخط المتوسط، وضغط ≥3 مواضع متتالية إلى مدى «1-8»؛
  الثنائي يبقى صريحاً «1,2» (معرفة رقم السن فوراً أولوية المالك).
- **بطاقة الملف**: خلايا `ToothLabelView` الحقيقية (خطا الربع بلون
  ربعه) على المجموعات — فمٌ كامل = **4 خلايا** بدل 32 رقاقة مجهرية؛
  FDI مدى رقمي كامل «11-18» بلا إطار.
- **الطباعة**: خلية توأم في PDF (`_pwToothGroupCell`) بحدود الربع نفسها
  وLTR صريح داخل النص العربي — نفس التلخيص حرفياً في ملف المريض المطبوع.
- اللبني: «A-E» (وFDI لبني «51-55»)، والإطباق المختلط مجموعتان بالربع.

**الاختبارات**: `m104_tooth_summary_test` — 13: وحدة التجميع بكل حالاتها
(فم كامل، متفرق، ثنائي، لبني، مختلط، FDI، شواهد، تلف) + واجهة بطاقة
الملف (فم كامل = 4 خلايا مدى في Palmer وFDI). تحديث `m11` (دخان PDF
بالخلايا الجديدة) و`m101` (مجموعة الربع «13,16»). عرضٌ محض — صفر تغيير
تخزين.

---

# سجلّ م103: أسنان المعالجات الدَّينية كانت «تختفي» (2026‑08‑02)

## م103 (البناء 103) — إصلاح جذري: أسنان المعالجة الدَّينية غير مرئية

**البلاغ**: معالجة جديدة بأسنان محددة تُحفظ، ثم لا تظهر في بطاقة ملف
المريض، وإعادةُ فتح «تحديد الأسنان» تجده فارغاً.

**السبب الجذري (مثبت من صفوف الخادم الحي + الشيفرة)**: المعالجة الدَّينية
تُنشئ ثلاثة صفوف: سجل أصل الدين (isDebt — **يحمل الأسنان**)، والدين،
وسجل «دفعة أولى (دين)». قائمةُ الملف **تخفي أصل الدين** (توافق Vue:
القيود بلا أصل الدين)، وقارئُ الأسنان `_resolveTeeth` يقرأ **الدين أولاً**
ثم الصف نفسه — وكلاهما كان **بلا نسخة تقرير** (منشئ المعالجة لم يكن
ينسخها للدين، بعكس Vue). فالأسنان كانت تُحفظ سليمةً على الصف المخفي
وحده وتبدو «مختفية». المزامنة بريئة — الخادم كان يخزّن كل ما دُفع إليه
حرفياً.

**الإصلاح الثلاثي**:
1. **`record_saver`** — نسخة التقرير تُكتب على الدين عند الإنشاء
   (فرعا العادي والتركيبات) — عودة لتكافؤ Vue.
2. **`_resolveTeeth`** — ملاذٌ أخير يصعد عبر `debt.recordId/prostheticId`
   إلى سجل الأصل المخفي ويقرأ تقريره: **شفاء رجعي** لكل البيانات
   القائمة بلا أي تعديل بيانات (عرض محض).
3. **محرر أسنان الملف** — التحرير من بطاقة «دفعة» ظاهرة يحدّث أيضاً سجل
   الأصل المخفي، فلا تتباعد النسخ الثلاث أبداً (تباعدٌ رُصد فعلاً على
   الخادم قبل الإصلاح: دينُ «خلع» بأسنانٍ تخالف سجلَّه).

**الاختبارات**: `m103_debt_teeth_visibility_test` — حارسان: الدورة
الكاملة (إنشاء دَيني ← بطاقة ظاهرة بالرقاقات ← محدد ممتلئ ← تعديل يوحّد
النسخ الثلاث) + الشفاء الرجعي لدينٍ قديم بلا نسخة. الحزمة كاملة خضراء.

---

# سجلّ م100: نظام ترقيم الأسنان Palmer/FDI (2026‑08‑01)

## م100 — إعادة تصميم عرض ترقيم الأسنان (بحث ← تحليل ← تنفيذ)

بحثٌ موثّق من **ISO 3950:2016** وFDI وADA (تقريرا المرحلتين محفوظان)،
ثم تنفيذٌ يحمي التوافق تماماً.

**المبدأ المعماري (يحمي كل البيانات):** التخزين المحايد `{q,n}` (ربع +
موضع من المنتصف — بنية ISO نفسها) **لم يتغيّر بايتاً**. Palmer/FDI طبقةُ
عرضٍ نقيّة فوقه.

**ما نُفّذ (طبقة العرض + الإعدادات):**
1. **`tooth_notation.dart` (جديد)** — دوال عرض نقيّة: Palmer (الموضع
   1–8 / لبني A–E مع إطار الربع) وFDI (رقمان 11–48 دائم، 51–85 لبني)
   حسب ISO 3950 حرفياً. بنية قابلة لإضافة Universal/نظام لبني آخر بفرع واحد.
2. **`tooth_label_widget.dart` (جديد)** — خليّة Palmer مطابِقة لمرجع Vue
   بالبكسل (حدود 2px حسب الربع، 15px وزن 800، حشو 3×7، LTR) أو نص FDI.
3. **حوار الأسنان** — رقائق الأسنان صارت تعرض الرمز الصحيح بدل المفتاح
   الخام `UR:6` (كان هذا «غير الدقيق» الذي رصده المالك).
4. **بطاقة المريض + تصدير ملفه** — العرض بالنظام المُزامَن.
5. **خيار الإعدادات «نظام ترقيم الأسنان»** (FDI / Palmer) في قسم المظهر
   والعرض — **مُزامَن** في app.config (هوية عرض للعيادة)، لا يمسّ البيانات.

**ما لم يتغيّر:** التخزين، المزامنة، القاعدة، الأشعة، الخطة العلاجية،
الألوان والأسلوب. البيانات القديمة تعمل فوراً (غياب وسم اللبني = دائم).

**الاختبارات:** `m100_tooth_notation_test` (13 وحدة تطابق كل خرائط ISO)
+ `m100_tooth_notation_ui_test` (الحوار FDI/Palmer). الحزمة كاملة خضراء.

## م100/7 (البناء 101) — الطقم اللبني: زر «دائم/لبني» ووسم `d:'P'`

**المخطط** (`tooth_chart.dart`): هندسة لبنية ثانية `primaryToothData` —
20 سناً (A–E لكل ربع) على نفس اللوحة 320×215 والأقواس، بنِسَبٍ مشتقة من
مخطط البالغ (قوس أقصر تشريحياً، أرحاء D/E أعرض، تناظر مرآتي حول المنتصف
مُختبَر). معاملا `dentition`/`system` إضافيان بافتراضٍ يطابق السلوك
التاريخي حرفياً، ورمزُ الخلية يتبع نظام العرض (حرف لبني/رقما FDI).

**الحوار**: زر تبديل «دائم/لبني» (SegmentedButton مضغوط فوق المخطط —
نفس نمط الإعدادات). الاختيار مجموعة واحدة تجمع النوعين (**إطباق مختلط**
يوثَّق في زيارة واحدة)، وفتحٌ ذكي: ملفٌ كل أسنانه لبنية يفتح على اللبني.

**المفاتيح والتخزين (قرار التوافق المعتمَد)**: مفتاح اللبني `q:n:P` —
لاحقة إضافية بحتة (مفاتيح الدائم لا تتغير حرفاً)، والحفظ يكتب وسم
`d:'P'` **للّبني فقط**: صفوف الدائم تبقى `{q,n}` بايتاً ببايت، وغياب
الوسم = دائم، فكل البيانات القديمة والمزامنة والنسخ الأقدم سالمة.

**العرض في كل المواضع**: بطاقة المريض وتصديرُه وPDF التقرير تقرأ الوسم
وتعرض الرمز الصحيح (Palmer حرف A–E بإطار الربع / FDI أرباع 5–8).

**الاختبارات**: `m100_primary_dentition_test` — 13 اختباراً: المفاتيح
والتفكيك الآمن، هندسة العشرين والتناظر، تدفق الحوار كاملاً (تبديل، نقر،
رقاقة، إلغاء، حفظ موسوم، إطباق مختلط، فتح ذكي، FDI لبني)، وتوفيقٌ يحفظ
الوسم. الحزمة كاملة خضراء.

---

# سجلّ م99: قسم «كشف مالي» في المالية (2026‑08‑01)

## م99 — الكشف المالي صار قسماً دائماً بجانب الخزينة/الديون/الأرباح

**طلب المالك**: مكان الكشف (اللوحة السفلية) غير مرتب — بطاقة رابعة «كشف
مالي» في المالية، بالكشف الإجمالي للعيادات + فلترة متعددة (عيادة/كاش/
تحويل/تركيبات) + مدى من→إلى + إجمالي أسفل + طباعة/تصدير + بحث، بنفس
أسلوب تفصيل الخزينة.

**التنفيذ**:
1. **دالة `financialStatement`** (home_logic): كشفٌ بمدى `[from..to]` +
   فلترة **متعددة الاختيار** بالعيادات والفئات (كاش/تحويل/تركيبات/دين +
   أي طريقة دفع) + بحث بالاسم، مجموعاً حسب العيادة والأحدثُ أولاً، بنفس
   قواعد م23.
2. **قسم `StatementSection`** رابعاً في `fin-seg-statement`: بحث ممتد +
   زر طباعة، حقلا مدى «من/إلى» بتقويمين، رقائق فلترة متعددة للعيادات
   والفئات، قائمة مجموعة بالعيادة (الأحدث أولاً)، وشريط إجمالي ثابت
   أسفل يتبع كل الفلاتر.
3. **طباعة/تصدير PDF** عبر `simpleTablePdf` + `printOrSharePdf` بنفس
   هوية طباعة المشروع، بالعنوان والمدى والفلاتر والجدول والإجمالي.
4. بطاقة «دخل اليوم» في الرئيسية بقيت كما هي (اللوحة السفلية م98 تعمل
   كمعاينة سريعة)، والكشف الموسّع صار في المالية.

**ما لم يتغيّر**: تعريف م23، الخزينة/الديون/الأرباح، المزامنة/القاعدة.

**الاختبارات**: `m99_statement_test` (وحدات: المدى، الفلترة المتعددة،
البحث، الأحدث أولاً) + `m99_statement_ui_test` (واجهة: التنقّل، الرقائق
الحيّة). الحزمة كاملة خضراء.

---

# سجلّ م98: كشف الحساب بمدى تاريخي (2026‑08‑01)

## م98 — لوحة «دخل اليوم» صارت «كشف حساب» بمدى وفلاتر

**طلب المالك** (نقطتان + استشارة): الأحدث إضافةً يظهر أعلى دائماً؛ زر
فلترة بالعيادة/الدفع؛ ومراجعة حسابات أي فترة — «كشف حساب من تاريخ كذا
إلى كذا».

**التنفيذ**:
1. **دالة نقية `incomeByClinicRange`** (home_logic): دخل مدى `[from..to]`
   بنفس قواعد م23 حرفياً، + فلترة اختيارية بالعيادة وطريقة الدفع، +
   **فرز الأحدث إنشاءً أولاً** (على `createdAt`). `todayIncomeByClinic`
   صارت غلافاً لليوم الحالي فوقها — فتعريف م23 واختباراته بلا مساس.
2. **لوحة `_IncomeStatementSheet`** (StatefulBuilder): حقلا «من/إلى»
   بتقويمين (الأقصى اليوم، وإلى ≥ من)، الافتراض من=إلى=اليوم فيبقى الفتح
   المعتاد «دخل اليوم». مدُّ المدى يقلب العنوان إلى «كشف حساب من س إلى ص»
   ويُظهر تاريخ كل صف. منسدلتا العيادة والدفع تتراكبان، والإجماليات (لكل
   عيادة والكلي) تتبع المدى والفلاتر. مفتاح الإجمالي `sheet-income-total`
   كما هو.

**ما لم يتغيّر**: تعريف الدخل (م23)، بطاقة «دخل اليوم» في الرئيسية
ورقمها، والمزامنة/القاعدة (قراءة وعرض فقط).

**الاختبارات**: `m98_income_statement_test` (وحدات: المدى، الفلاتر،
الأحدث أولاً، قواعد م23) + `m98_income_statement_ui_test` (واجهة: الفتح،
الفلترة الحيّة). الحزمة كاملة خضراء.

---

# سجلّ م97: نموذج القفل النهائي (مطفأ افتراضاً + رمز إجباري) (2026‑08‑01)

## م97 — القفل خدمةٌ يفعّلها المالك برمزٍ إجباري

**خطة المالك**: المفتاحان («القفل عند فتح التطبيق» + «الدخول بالبصمة»)
مطفآن افتراضياً للجميع، وتشغيل أيٍّ منهما يستلزم تعيين «رمز قفل» إجبارياً.

**التغييرات**:
1. **الافتراض انقلب**: `lockOnStartEnabled` الغياب ⇒ **معطَّل** (كان مفعَّلاً/م87). القفلُ كلُّه — إقلاعاً **وخمولاً** — محكومٌ بهذا المفتاح الواحد: مطفأٌ ⇒ لا قفل إطلاقاً.
2. **بوابة موحّدة للمفتاحين** (`_requirePinForToggle`): تشغيل القفل أو البصمة بلا رمزٍ معيَّن يفتح حوار تعيين الرمز؛ الإلغاء يُبقي المفتاح مطفأً برسالة، والتعيين يفعّله. ينطبق على الجميع — كلمةُ المرور مفتاحُ الحساب، والرمزُ مفتاحُ الشاشة (كالتطبيقات المعتادة).
3. **حماية الرمز من الدهس**: دخولُ كلمة المرور يخزّن مُتحقِّقها **فقط إن لم يوجد رمزٌ معيَّن** — فلا يُمحى رمزُ المستخدم عند كل إعادة دخول.
4. **العرض الفعّال**: المفتاحان لا يظهران مفعَّلَين إلا برمزٍ معيَّن.
5. البصمة مطفأة افتراضاً (قائم)، ونصّها: «يتطلّب تعيين رمز قفلٍ أولاً».

**أثر الترقية**: من لم يلمس المفتاح قط سيجده مطفأً بعد الترقية (الأمان يُفعَّل بنقرةٍ + تعيين رمز). مستخدم كلمة المرور: القفل صار اختيارياً له أيضاً.

**الاختبارات**: `test/m97_lock_defaults_test.dart` (جديد) + تحديث م86/م87/م91/م96 لتفعّل القفل صراحةً حيث تختبره. الحزمة كاملة خضراء.

---

# سجلّ م96: لا قفلَ بلا وسيلة فتح (2026‑08‑01)

## م96 — منع فخّ «القفل بلا مفتاح» من أصله

**الملاحظة (المالك)**: الرسالة الإرشادية «لا وسيلة فتح — سجّل الخروج»
يجب ألّا تظهر أصلاً.

**السبب الجذري**: قفلُ الخمول (`idle_lock._check`) كان يشتعل بعد المهلة
**بلا أي شرط** على وجود وسيلة فتح — فمستخدم Google المتخطّي للرمز يُقفل
ويقابل شاشةَ «خروج فقط» ورسالتها. (قفلُ الإقلاع كان مشروطاً منذ م91،
لكن الخمول أُفلت.)

**الإصلاح — القاعدة: لا قفلَ بلا وسيلة**:
1. **`idle_lock._check`**: لا يقفل إلا إن وُجد `hasLockVerifier || biometricEnabled`. فمن لا رمز له ولا بصمة لا يُقفل خمولُه أبداً ⇒ الرسالة لا تظهر إطلاقاً (بقيت شبكةَ أمانٍ دفاعية غير مؤدّاة).
2. **مفتاح «القفل عند فتح التطبيق»** (`settings/_setLockOnStartGated`): تفعيلُه بلا وسيلة فتح يقود لحوار تعيين الرمز أولاً، ولا يُفعَّل إلا إن عُيّن؛ الإلغاء يُبقيه مطفأً برسالة. وعرضُه الفعّال «مطفأ» لمن لا وسيلة له مهما كان التفضيل المخزَّن.
3. **أول دخول Google**: أُزيلت نافذة «أنشئ رمز قفل» التلقائية (`post_login_gate._maybeOfferPin`) — لا مطالبةَ عند الدخول؛ التعيينُ من «إعدادات الحساب» متى شاء.
4. البصمة مطفأة افتراضاً (قائم من م87).

**النتيجة**: داخلُ Google يعمل بلا أي قفلٍ يحبسه وبلا نافذةٍ تلاحقه؛ ومن
أراد الأمان يعيّن رمزاً فيعمل القفل كاملاً. مستخدم كلمة المرور: صفر تغيير.

**الاختبارات**: `test/m96_no_lock_without_key_test.dart` (جديد) +
تحديث م91 المتأثرة. الحزمة كاملة خضراء.

---

# سجلّ م95: خروجٌ مباشر بلا وميض (2026‑08‑01)

## م95 — الخروج من الإعدادات يهبط على شاشة الدخول مباشرة

**الملاحظة (المالك)**: «عند تسجيل الخروج يرجعني لآخر واجهة كنت فيها ثم
يخرج لشاشة تسجيل الدخول».

**العلة**: تنفيذ الخروج كان يغلق شاشةَ الإعدادات أولاً (فتظهر آخر واجهة —
الصدفة) ثم ينتظر 300ms ثم يسجّل الخروج فتحلّ شاشة الدخول.

**الإصلاح**: عُكس الترتيب — الخروج يتم وشاشةُ الحجب ما تزال فوق
الإعدادات، ثم تُزال المسارات كلها حتى الجذر دفعةً واحدة (`popUntil
isFirst` — نفس نمط حذف الحساب م94)، وأُلغيت مهلة 300ms. مسار الإجهاض
(فشل مزامنة/رفع) كما هو: البقاء في الإعدادات برسالة.

**الحارس**: `test/m95_logout_direct_test.dart` — رقابة إطاراً بإطار: لا
إطارَ يخلو من هيدر الإعدادات ومن شاشة الدخول معاً (الفجوة القديمة
بعينها)، والإلغاء يبقي المستخدم حيث هو.

---

# سجلّ م94: صقل إعدادات الحساب و«حول التطبيق» (2026‑08‑01)

> هذا الملف خلاصةُ آخر دفعات التعديل. سجلُّ قيم Google/Supabase من دفعة
> م88 **محفوظ أدناه كما هو** — لا يحتوي أي سرّ.

---

## م94 — صقل ما بعد التجربة الحية (ملاحظات المالك، نُفِّذت كلها)

| # | الملاحظة | التنفيذ |
|---|---|---|
| ١ | «بعد الحذف لازم يرجع تلقائياً لواجهة تسجيل الدخول» | نجاحُ الحذف يزيل كل المسارات حتى الجذر (`popUntil isFirst`): تغيّرُ حالة المصادقة كان يبدّل الجذرَ لكن الإعدادات وحوارَ الحجب مساران مكدّسان فوقه لا يزيلهما — الآن يهبط المستخدم على شاشة الدخول مباشرة |
| ٢ | «لا تذكر أسماء الخوادم في الإعدادات» | نصُّ بطاقة الحذف صار: «يُمحى حسابك بكامل بياناته وصوره من السحابة ومن هذا الجهاز» — ونصُّ الأرشفة الباردة: «يُحزَم إلى التخزين السحابي». **قاعدة دائمة**: لا أسماء بنية تحتية في أي نص واجهة؛ الرسائل بمفاهيم المستخدم (حسابك، بياناتك، صورك، السحابة) |
| ٣ | «أزل بطاقة البريد فوق تسجيل الخروج» | أُزيلت بالكامل — البريد وإدارته داخل قسم «إعدادات الحساب» |
| ٤ | «قسم حول التطبيق تحت إعدادات الحساب يضم الحالة والتواصل» | قسمٌ جديد «حول التطبيق» (الثاني عشر) أسفل «إعدادات الحساب» يضم بطاقتي «حالة التطبيق والمزامنة» (v28) و«التواصل مع المطور» — **فلا يبقى أسفل القائمة إلا زرّ تسجيل الخروج** |

ملفات م94: `account_section.dart` (العودة للجذر + النص المحايد)،
`settings_screen.dart` (حذف بطاقة البريد + قسم `about` + `_aboutBody` +
نص الأرشفة). **الاختبارات: 774 خضراء، صفر انحدار.**

---

## م93 — قسم «إعدادات الحساب»

قسمٌ جديد أسفل «إعدادات المختبرات» في قائمة الإعدادات (للحسابات السحابية
وحدها)، وبطاقةُ البريد أسفل القائمة صارت تفتحه بالنقر. يضمّ ثلاثاً:

### ١) البريد
عرضُ بريد الحساب + نسخ.

### ٢) كلمة المرور — خدمة ذاتية عبر GoTrue
- **مستخدم Google** (بلا كلمة مرور): «تعيين كلمة مرور» — يضيف هوية email
  فيدخل بالبريد أيضاً. بلا قديمة (الجلسة إثبات الهوية).
- **مستخدم له كلمة مرور**: «إعادة تعيين» **تشترط القديمة** — نتحقق منها
  بإعادة مصادقةٍ حقيقية (`signInWithPassword`) قبل `PUT /auth/v1/user`،
  فلا تُغيَّر كلمةُ مرورِ جلسةٍ مسروقةٍ دون معرفة الحالية.
- تحديد الحالة عبر `GET /auth/v1/user` (مصفوفة identities). كلُّ ذلك
  بمفتاح anon + رمز الجلسة — لا سرّ إداري.

### ٣) حذف الحساب — حقيقيٌّ من Supabase وR2
- حوارُ تأكيدٍ بعدّاد **5 ثوانٍ** يعطّل زرَّ «حذف نهائي» حتى ينقضي —
  فلا نقرةَ اندفاعٍ تمحو حساباً.
- التدفّق (ترتيبٌ مقصود): **RPC أولاً** (`delete_my_account`) — الخادم
  مصدر الحقيقة — ثم **تطهير R2** (تعداد كل `file_key` محلياً وحذفها،
  أفضل جهد؛ الرمز يبقى صالح التوقيع بعد الحذف حتى انتهائه) ثم **مسحٌ
  محليّ كامل + خروج** (نفس مسار مسح تبديل الحساب + مسح مُتحقِّق القفل).

> **⚠ يتطلب تطبيق المهاجرة `0031_delete_my_account.sql`** (كما طُبّقت
> 0030): مفتاح anon لا يحذف صفَّ `auth.users` بالتصميم، فالحذف يعتمد
> دالةً `SECURITY DEFINER` تحذف بيانات المستدعي ثم صفَّه. **قبل تطبيقها**
> يعيد الخادم PGRST202 فيُجهض التطبيقُ الحذفَ **بنظافة** برسالة عربية
> صريحة — لا حالة نصف محذوفة. خطوات التطبيق في `ACCOUNT_DELETE_AR.md`.

### ما لم يتغيّر
الوضع المحلي (بلا Supabase) لا يرى القسم أصلاً — صفر تغيير على مساره.
كلُّ المنطق معزولٌ في `AccountAdmin` ويُختبَر بمزيّفات (لا مساس بمشروع
الإنتاج، ولا اعتماد إداري في التطبيق).

### الملفات والاختبارات
| الملف | الدور |
|---|---|
| `supabase/migrations/0031_delete_my_account.sql` | **جديد** — دالة الحذف الذاتي (يطبّقها المالك) |
| `lib/data/cloud/gotrue_client.dart` | `userProviders` + `updatePassword` + `deleteMyAccount` + نوع `AccountRpcMissing` |
| `lib/features/auth/account_admin.dart` | **جديد** — تنسيق كلمة المرور والحذف (معزول للاختبار) |
| `lib/features/settings/account_section.dart` | **جديد** — واجهة القسم + حوار العدّاد |
| `lib/app/providers.dart` | `accountAdminProvider` + `deleteLocalAndLogout` |
| `lib/features/settings/settings_screen.dart` | القسم الحادي عشر + بطاقة بريدٍ قابلة للنقر |
| `test/m93_account_settings_test.dart` | **جديد** — 22 اختباراً (منطق + واجهة، بمزيّفات) |

---

## م92 — شاشة الإعدادات بنمط تطبيقات المراسلة

### المطلوب (لقطة مرجعية من المالك)
كانت الإعدادات عشرَ مجموعاتٍ قابلة للطي (أكورديون) في صفحة واحدة طويلة.
صارت **قائمة رئيسية**: صفٌّ لكل قسم (أيقونة داخل قرصٍ بلون العلامة +
عنوان عريض + سطر وصفٍ رمادي يلخّص محتواه فعلياً)، والنقر يفتح **صفحة
قسمٍ مستقلة** بهيدر التدرج نفسه وزرِّ رجوع — وبهوية التطبيق البصرية لا
بألوان المرجع.

### القرار الهندسي — تبديلٌ داخل نفس كائن الحالة لا Route
أجسام الأقسام تتشارك حالة الشاشة بكثافة (متحكمات، عقد تركيز م55، دوال
حفظ). دفعُ Route منفصل يقطع setState عن الصفحة المدفوعة، **فالتبديل
داخلي**: حقل `openSection` واحد يقرر ما يُعرض، وكل شيفرة الأقسام بقيت
حرفياً بلا تغيير. زرُّ رجوع النظام يغلق القسم أولاً (PopScope) كسلوك
تطبيقات المراسلة، والانتقال بتلاشٍ وانزياحٍ خفيف (220ms).

### شبكة أمانٍ جديدة اكتشفتها اختبارات م55
مغادرة القسم بالتبديل الداخلي لا تمرّ بتفكيك المسار الذي كانت تعتمده
شبكة أمان أسعار المعالجات (الحفظ عند فقد التركيز)، بل وقد تسبق اكتمالَ
طلب التركيز نفسه (كتابةٌ ثم مغادرة فورية) فلا يقع فقدُ تركيزٍ أصلاً.
فصار **إغلاق القسم يلتزم بكل سعرٍ معلّقٍ قبل الخروج** (`_closeSection`)
— والالتزام يكتب فقط عند تغيّر حقيقي، فلا ضجيج ولا صفوف مزامنة عبثية.

### ما لم يتغيّر
كل وظيفة بقيت كما هي حرفياً: نفس المفاتيح (`group-*`، `settings-header`،
كل مفاتيح الحقول والأزرار)، نفس الحفظ والتحقق والنصوص داخل الأقسام،
بطاقة الحساب وزر الخروج وبطاقة حالة التطبيق أسفل القائمة الرئيسية.

### الملفات والاختبارات
| الملف | التغيير |
|---|---|
| `lib/features/settings/settings_screen.dart` | `openSection` + سجلّ الأقسام + `_hubRow` + الهيدر الديناميكي + `_closeSection` (بدل `open` و`_group`) |
| اختبارات التنقل | تحديث عشرة ملفات من «فتح/إغلاق الأكورديون» إلى «دخول/رجوع» (مفتاح جديد `settings-section-back`) — **كل تأكيدات الوظائف كما هي** |

---

## م91 — سدّ فخّ «قفلٌ بلا مفتاح» لمن يدخل بحساب Google

### الفخ (رصده المالك)
داخلُ Google لا تمرّ كلمةُ مروره بالتطبيق أبداً (جوهر أمان OAuth)، فلا
يُخزَّن له «مُتحقِّق قفلٍ» محلي. النتيجة كانت ثلاثيةً:
1. شاشة القفل تعرض له **«تسجيل الخروج» فقط** — لا حقل ولا زر فتح.
2. زرُّ البصمة نفسه كان مشروطاً بوجود المُتحقِّق فلا يظهر حتى مع تفعيلها —
   بينما توثيق م88 كان يَعِد بعكس ذلك حرفياً (تناقضٌ أثبت أن الفخ سهو).
3. «القفل عند فتح التطبيق» في الإعدادات يبدو مفعَّلاً لكنه **يتخطّى
   صامتاً** (شرط المُتحقِّق في بوابة الإقلاع) — أمانٌ شكلي.

### الحل — ستة بنود + تقويتان
| # | البند | الموضع |
|---|---|---|
| ١ | **«رمز قفل التطبيق»**: رمز رقمي (4+) خاص بالجهاز، تحت **نفس مفتاح المُتحقِّق** (`auth.lock_verifier`) بمغلّف PBKDF2 بملح + وسم نوعٍ جديد (`auth.lock_verifier_kind`: `password`/`pin`، الغياب=`password`). فكل ما بُني فوق المُتحقِّق يعمل تلقائياً: قفل الإقلاع، الفتح بلا شبكة، مسح تبديل الحساب، خروج شاشة القفل | `idle_lock.dart` |
| ٢ | **اقتراح تعيينه فور دخول Google**: حوار (رمز+تأكيد+«لاحقاً») بعد بلوغ الصدفة. قاعدة العرض `دخول صريح && لا مُتحقِّق` — مسار كلمة المرور لا يراه أبداً بنيوياً | `post_login_gate.dart` + `pin_setup.dart` (جديد) |
| ٣ | **تحرير البصمة من شرط المُتحقِّق**: تُعرض وتعمل وحدها — مصادقة النظام حجة كافية | `idle_lock.dart` |
| ٣ب | **قفل الإقلاع يشمل حالة البصمة-فقط**: `(مُتحقِّق ‖ بصمة) && المفتاح مفعَّل` — كان يتخطى صامتاً | `post_login_gate.dart` |
| ٤ | **بند إعدادات «تعيين/تغيير رمز القفل»** في قسم قفل الدخول — يظهر لمن لا مُتحقِّق له أو مُتحقِّقُه رمز؛ صاحبُ كلمة المرور لا يرى شيئاً جديداً | `settings_screen.dart` |
| ٥ | **تنبيه صادق**: القفل مفعَّل بلا أي وسيلة فتح ⇒ تحذير كهرماني + زر تعيين فوري | `settings_screen.dart` |
| ٦ | **نص إرشادي في شاشة القفل** لمن بلا وسيلة: الخروج آمن + إرشاد لتعيين الرمز بعد الدخول | `idle_lock.dart` |
| + | **خنق محاولات الغطاء**: 5 إخفاقات ⇒ 30 ثانية (كان الغطاء بلا أي خنق — حتى لكلمة المرور)، في الذاكرة فقط كنمط م68 | `idle_lock.dart` |
| + | **تطبيع الأرقام**: ٠-٩/۰-۹ ⇒ 0-9 عند التخزين والتحقق — لوحة المستخدم لا تغيّر رمزه | `idle_lock.dart` |

### ما لم يتغيّر — ضمانات
- **مسار البريد+كلمة المرور: صفر تغيير سلوكي** — نقطة الكتابة الوحيدة كما
  هي، نصوص الشاشة كما هي (تحرسها م86/م87/م88 بلا أي تعديل عليها).
- **لا مساس بالمزامنة/الخادم/القاعدة** — الرمز في `sync_meta` المحلية،
  لا يدخل أي حمولة، ويُمسح عند تبديل الحساب (يمسحها `wipeAllAccountData`).
- **التشفير (م83) كما هو** — الرمز قفلُ شاشةٍ فوقه لا بديلٌ عنه.
- مفاتيح الاختبار القائمة (`idle-lock-*`) بأسمائها — لا كسر لأي اختبار.

### الاختبارات — `test/m91_google_lock_pin_test.dart` (جديد، 15 اختباراً)
تثبت الفخ القديم أولاً (بلا وسيلة: لا حقل ولا بصمة — لكن بإرشادٍ صادق
وخروجٍ يعمل) ثم تحرس الحل: التخزين بوسمه والأرقام العربية، قواعد العرض
الثلاث، البصمة وحدها (نجاحاً وفشلاً بلا حبس)، الحوار (حفظاً وتخطياً)،
إقلاعٌ مقفل يُفتح بالرمز، مسارُ كلمة المرور حرفاً بحرف، بند الإعدادات
وتنبيهها ظهوراً وانطفاءً، والخنق صدّاً وانقضاءً.

### ملفات م91
| الملف | التغيير |
|---|---|
| `lib/features/auth/idle_lock.dart` | الوسم + `storeLockPin`/`lockVerifierKind`/التطبيع/الصحة + تحرير البصمة + واجهة الغطاء الصادقة + الخنق |
| `lib/features/auth/pin_setup.dart` | **جديد** — حوار التعيين المشترك + قواعد العرض الثلاث |
| `lib/features/auth/post_login_gate.dart` | شرط الإقلاع الموسَّع + اقتراح الرمز بعد البوابة |
| `lib/features/settings/settings_screen.dart` | بند التعيين/التغيير + التنبيه الصادق |
| `lib/app/providers.dart` | تصحيح توثيق م88 المتناقض (تعليق فقط — لا شيفرة) |
| `test/m91_google_lock_pin_test.dart` | **جديد** — 15 اختباراً |

---

# سجلّ م88 — تسجيل الدخول بحساب Google (محفوظ)

> **سجلُّ القيم التي أُضيفت في حساب Google وSupabase** لتعمل الميزة —
> حتى لا تضيع ولا يُعاد اكتشافها. لا يحتوي أي سرّ (الأسرار في الخادم
> وملفات البيئة المستثناة).

---

## ١) القيم المسجَّلة في حساب Google Cloud (أنشأها المالك)

### شاشة الموافقة (Google Auth Platform)
| البند | القيمة |
|---|---|
| نوع الجمهور | External |
| ملاحظة النشر | إن ظهر `access_denied` لحسابٍ غير حساب المالك ⇒ نفّذ **PUBLISH APP** من صفحة Audience |

### عميل OAuth — نوع **Web** (يستعمله خادم Supabase)
| البند | القيمة |
|---|---|
| Client ID | `983041345831-008vf0v3avl6choovqe83vi5jj9hhmdh.apps.googleusercontent.com` |
| Client Secret | ⚠️ **لا يُكتب هنا** — مخزَّن في Supabase (مزوّد Google)، ويُعرض عند الحاجة من Google Console ← Clients |
| Authorized redirect URI | `https://qajgqatflmiiwqznxfha.supabase.co/auth/v1/callback` |

### عميلا OAuth — نوع **Android** (للنافذة الأصلية داخل التطبيق)
| Package name | SHA‑1 | الغرض |
|---|---|---|
| `com.dental.clinic.debug` | `01:77:DE:51:F8:65:7A:62:A0:44:A0:B6:51:04:0C:7D:89:0F:B0:EB` | النسخة التجريبية الحالية (توقيع مفتاح التطوير `dental_dev.jks`) — **مُتحقَّق: النافذة الأصلية تعمل** |
| `com.dental.clinic` | نفس البصمة أعلاه مؤقتاً | النسخة النهائية مستقبلاً — ⚠️ عند إنشاء مفتاح توقيع النشر الحقيقي (`android/key.properties`) **يجب تحديث بصمة هذا العميل** ببصمة المفتاح الجديد وإلا سقط الدخول للمتصفح |

> استخراج البصمة عند الحاجة:
> `keytool -list -v -keystore android/app/dental_dev.jks -alias dental`
> (كلمتا المرور في `tool/dev_keystore.env` — خارج git وخارج هذا الأرشيف.)

## ٢) القيم المضبوطة في Supabase (خادم `qajgqatflmiiwqznxfha`)

| البند | القيمة |
|---|---|
| مزوّد Google | مفعَّل، بمعرّف عميل الويب وسرّه أعلاه |
| Redirect allow‑list | `dentaldk://login-callback` و`/` + `http://localhost:43823` و`http://127.0.0.1:43823` و`/` |
| جدول `profiles` | هجرة `0030` — إنشاء/تحديث تلقائي بتريغر، RLS مالكها `auth.uid()` |
| ربط الحسابات | تلقائي بنفس البريد (`mailer_autoconfirm=true`) — نفس المعرّف فلا تكرار ولا فقدان بيانات |

## ٣) قيم البناء (ملف `tool/cloud.env` — خارج git وهذا الأرشيف)

المفاتيح المطلوبة: `SUPABASE_URL`، `SUPABASE_ANON_KEY`، `R2_WORKER`،
`PHONE_IDENTITY`، `COLD_FETCH`، **`GOOGLE_WEB_CLIENT_ID`** (الجديد في م88/ج
= معرّف عميل الويب أعلاه؛ عامٌّ بطبعه). انسخ `tool/cloud.env.example`
واملأه، ثم: `./tool/build_cloud.sh trial <رقم_البناء>`.

## ٤) خلاصة ما تغيّر في الشيفرة (م88 بأجزائها الثلاثة)

- **أ — الجسر والبوابة:** عقد `OAuthSignIn` + زرّ «المتابعة باستخدام
  Google» + توجيه الجديد لإعدادٍ إجباري والعائد لترحيبٍ باسمه.
- **ب — الحزمة الرسمية:** `supabase_flutter 2.16.0` بتدفّق **PKCE** نصاً؛
  تخزين جلسة Google **و**مُتحقِّق PKCE **و**جلسة كلمة المرور كلها في مخزن
  أسرار النظام (Keystore/DPAPI) — **لا رموز بنصٍّ واضح**، مع ترحيلٍ
  تلقائي من `session.json` القديم يحافظ على بقاء المستخدم داخلاً؛ تجديد
  تلقائي + مسار 401؛ قاعدة «محرّك جلسة واحد»؛ deep‑link أندرويد
  (`dentaldk://login-callback`، ‏launchMode=singleTask، ‏minSdk 23→24)
  وloopback ويندوز (منفذ 43823)؛ إقلاعٌ سحابي يتدهور رشيقاً.
- **ج — ملاحظات المالك بعد التجربة:** النافذة الأصلية (google_sign_in v7
  ⇒ `signInWithIdToken`) بدل المتصفح مع سقوطٍ رشيق إليه؛ ورابط «تسجيل
  الخروج» الصغير في شاشة الإعداد الإجباري (يصمد الإجبارُ بعد العودة).

**الاختبارات: 723/723 خضراء · المحلل: 0 أخطاء 0 تحذيرات.**
**النسخ المسلَّمة:** v1.0.5 (متصفح، مُتحقَّقة على الهاتف) ثم v1.0.6
(النافذة الأصلية، مُتحقَّقة على الهاتف).

التفصيل الكامل بالملفات والاختبارات: `GOOGLE_SIGNIN_AR.md`.

---

# م89 — إصلاح فتح بطاقة المريض الخطأ عند تشابه الأسماء (2026‑08‑01)

**العطل (كما وصفه المالك):** مريضان بنفس الاسم والكنية في عيادتين. الإضافة
والدين يُسجَّلان في العيادة الصحيحة، لكن النقر على الاسم من تبويب الديون في
المالية كان يفتح بطاقة المريض في **العيادة الخطأ**.

**الجذر:** مُعالِج فتح الملف كان يتلقّى الاسمَ وحده، ثم يُعيد استنتاج
العيادة بمسح كل الديون والتوقّف عند **أوّل تطابقٍ بالاسم** — فيخطف صفُّ
سميٍّ في عيادةٍ أخرى الوجهةَ.

**الإصلاح:** النقر صار يحمل **عيادة صفّ الدين المنقور نفسه** (الظاهرة أمام
المستخدم) إلى مُعالِج الفتح، بدل إعادة الاستنتاج بالاسم. المسحُ بالاسم بقي
مسارَ احتياطٍ للبيانات القديمة التي بلا عيادة فقط. لا تغيير في نموذج
البيانات ولا المزامنة ولا أي هجرة — إصلاح تنقّلٍ خالص.

**الملفات:** `debts_section.dart` (المُعالِج + نقرة الاسم + مُوائم حوار
الدفعات)، `installment_dialog.dart` (رد النداء صار يحمل العيادة).

**الجرد:** فُحصت كل مواضع «الفتح بالاسم». الموضعان الوحيدان اللذان يقفزان
مباشرةً لبطاقةٍ (نقرة اسم الدين + رابط الاسم في حوار الدفعات) أُصلحا.
البقية إمّا تمرّر عيادتها أصلاً (قوائم العيادة، إعادة التسمية) أو تفتح
**قائمة بحثٍ** يختار منها المستخدم (المواعيد، واحتياطي الديون) — وهي آمنةٌ
لأنها تعرض السميَّين معاً.

**قيدٌ موروثٌ موثَّق (خارج نطاق م89):** تشابه الاسم **داخل العيادة الواحدة
نفسها** يبقى غامضاً بالتصميم (شاشة الملف معنونة بالاسم والعيادة) — معالجته
تحتاج مفتاح هوية أعمق وقراراً منفصلاً.

**الاختبارات:** `m89_debt_jump_clinic_test.dart` (اختباران: نقر دين ع2
يفتح ع2، ونقر دين ع1 يفتح ع1) — أثبتا العطل قبل الإصلاح ونجحا بعده.
**الإجمالي: 727/727 خضراء، المحلل نظيف.** النسخة: v1.0.7.

---

# م90 — تمييز مريضَين بنفس الاسم داخل العيادة الواحدة (2026‑08‑01)

**قرار المالك (يحدّث م35 جزئياً):** كانت هوية العرض (اسم|عيادة) فيندمج
السميّان داخل العيادة الواحدة بطاقةً وملفاً. صار التمييز **برقم الهاتف** —
وهو أصلاً جزء هوية الخلفية (p:هاتف:اسم) — في **طبقة العرض والتنقل فقط**:
صفر مساس بالمزامنة أو الحمولات أو أي هجرة.

**قاعدة التقسيم الحتمية** (دالة مشتركة واحدة في `patients_logic.dart`):
- هاتفٌ مميز واحد أو لا شيء في مجموعة (اسم|عيادة) ⇒ هوية واحدة تضم الكل —
  سلوك م35 حرفياً، فلا يتغيّر أي مريض قائم غير متشابه.
- هاتفان مختلفان فأكثر ⇒ تنقسم: صفوف كل هاتف هوية مستقلة، والصفوف بلا
  هاتف هوية «بلا رقم» ثالثة (لا تخمين في إلحاقها).

**ما تغيّر للمستخدم:**
- بطاقة مستقلة لكل سميّ في القوائم، مع سطر هاتف صغير للتفريق البصري.
- فتحُ كل بطاقة يعرض ملفَّ هويتها وحدها: زياراتها وديونها وأموالها.
- شارة الدين على صاحب الدين لا على سميّه.
- القفز من تبويب الديون ونافذة الدفعات يحمل الهوية (امتداد م89).
- **حارس إدخال جديد:** حفظ زيارة باسمٍ قائم بهاتفٍ مختلف ⇒ حوار «سيُنشأ
  مريض جديد مستقل»؛ وباسمٍ قائم بلا هاتف بينما القائمُ له هاتف ⇒ حوار
  «نفس المريض أم أدخل هاتفاً للتمييز؟». الهوية المطابقة تمرّ صامتة.

**حدود موثَّقة بصدق (مشتركة بين السميّين داخل العيادة نفسها):**
- **صور الأشعة** (مفتاحها بالاسم في المخزن) و**الأرشفة** (اسم|عيادة)
  و**البطاقة الطبية/خطة العلاج** (اسم|عيادة) تبقى مشتركة بين السميّين —
  فصلُها يمسّ مخزن الخزنة ومفاتيح إعداداتٍ متزامنة، فهو قرار منفصل إن لزم.
- سميّان **بلا أي هاتف لكليهما** يبقيان مدمجين (لا بيانات تفرّق) —
  والحارس الجديد يمنع تكوّن هذه الحالة مستقبلاً.

**الاختبارات:** `m90_same_name_same_clinic_test.dart` — 11 اختباراً
(قاعدة التقسيم بحالاتها، انفصال البطاقات والملفات والأموال، القفز من
الديون، الحارس بحواريه، وعدم تغيّر الحالة القائمة بهاتف واحد).
**الإجمالي: 738/738 خضراء، والمحلل نظيف.** النسخة: v1.0.8.

## ٥) بنود مستقبلية مسجَّلة
1. تجربة loopback **ويندوز** على جهاز ويندوز فعلي.
2. **PUBLISH APP** في Google قبل توزيع النسخة على العيادات.
3. عند إنشاء مفتاح توقيع النشر: تحديث بصمة عميل `com.dental.clinic`.
4. (اختياري، مدفوع) Supabase Custom Domain لإخفاء نطاق الخادم في صفحة
   المتصفح الاحتياطية.
