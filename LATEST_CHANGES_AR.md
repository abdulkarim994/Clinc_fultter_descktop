# آخر التعديلات — م147: صقل نموذج الزيارة + إضافة/حذف التحاليل من الجدول وبطاقة المريض (2026‑08‑09)

## م147 — تعديلات المالك على نموذج الزيارة ونظام التحاليل

**بصري (نموذج الزيارة المكتبي):**
- زر «اليوم» انتقل إلى داخل حقل التاريخ (suffix صغير) بدل سطرٍ مستقل.
- «تحديد الأسنان» صار زراً أيقونياً صغيراً (46×42) بجانب حقل المعالجة،
  بشارة عدٍّ عند التحديد، والضغط المطوّل يمسح التحديد. «المعلومات الطبية»
  تبقى رقاقةً في ترويسة القسم.

**وظيفي — إضافة/حذف التحاليل من زيارةٍ قائمة (طلب المالك):**
- دالة مشتركة `addAnalysisToVisit` (record_saver) تكتب صف isAnalysis معزولاً
  مربوطاً بزيارةٍ قائمة بسعر الإعدادات الثابت وطريقةٍ مختارة — بنيةٌ مطابقة
  حرفياً لصف «زيارة جديدة».
- نافذة مشتركة `pickAnalysisPayment` (كاش/تحويل، موافق/إلغاء) في
  `analysis_actions.dart`، وحارسٌ مسبق: الميزة معطّلة/سعر ≤ 0 ⇒ إرشاد بلا فتح.
- قائمة سياق الصف (كمبيوتر + هاتف): بند «إضافة التحاليل» حين لا تحليل
  (records.add)، و«حذف التحاليل» حين يوجد (records.delete) — كلاهما خلف
  الصلاحيات، والسعر ثابت من الإعدادات، والطريقة تُسأل فقط.
- بطاقة المريض: بند «إضافة التحاليل الثلاثية» في قائمة الثلاث نقاط — يضيف
  لآخر زيارة للمريض (يظهر فقط عند تفعيل الميزة وصلاحية records.add).

**تشخيص «المعلومات المختصرة»:** مسار البيانات سليمٌ تماماً بالبرهان — الملاحظة
تُحفظ على صف الزيارة (record.notes) وتُقرأ عبر getAll بمفتاح r:id (نفس مصدر
عمود الجدول)، وحفظ نافذة التحرير ينبض كل مزوّدات النسخة فيتحدّث العمود فوراً.
أُضيف اختبارٌ يثبّت هذا المسار دائماً (m147_analysis_actions_test).

**العزل المالي بلا مساس:** كل صفوف التحاليل تبقى محروسةً خارج الأرباح والخزينة
وعدّادات المريض. المنطق والحفظ والمزامنة ونسخة الهاتف الأساسية بلا تغيير جوهري.

**الاختبارات:** analyze صفر • الحزمة الكاملة 1147 اختباراً خضراء محلياً (4
اختبارات م147 جديدة) • اللقطات الذهبية روجعت. بُني تحت إشراف المشرف بوكيلٍ
مساعدٍ لسطحي الهاتف وبطاقة المريض.

---

