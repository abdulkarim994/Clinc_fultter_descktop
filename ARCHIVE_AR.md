# الأرشفة الباردة التلقائية — م70 (+ احتفاظ المواعيد — م71)

> **تحديث م73 — النافذة صارت 3 أشهر مع حارس الجهاز الغائب.**
> السجلات والأشعة تُؤرشف بعد **90 يوماً** بدل 210 (قابلة للضبط من
> الإعدادات: 3/6/12 شهراً). المكسب المقيس: 15.4 ← 8.1 م.ب للحساب ⇒
> **31 ← 59 عيادة** على المجانية، و**2,000 عيادة بـ26$** على Pro.
>
> وما جعل التقليص آمناً — ثلاث إضافات في المهاجرة 0022:
> **(١) حارس المؤشر:** الأرشفة لا تحذف صفاً لم تبلغه كل الأجهزة النشطة.
> كل جهاز يبلّغ مؤشره مرة يومياً (`report_sync_state`)، والخادم يحسب
> أدنى مؤشر ويقف عنده. جهاز غائب ⇒ الأرشفة تتوقف عند حدّه وتستأنف حين
> يعود ويلحق. والأجهزة المهجورة (180 يوماً بلا ظهور) تُتجاهل كي لا يوقف
> جهاز تالف الأرشفة للأبد.
> **(٢) أفق الأرشيف:** `pull_changes` تعيد أعلى txid حُذف؛ الجهاز الذي
> يجد مؤشره تحته يعرف أن ثمة فجوة **ويسترجع تلقائياً** بلا زر يدوي.
> **(٣) إصلاح عدّ:** `archive_rows` كانت تعيد عدد **الكيانات** لا الصفوف
> (`count(*)` على `GROUP BY entity`) — عطل موروث من 0020 أخفاه اختبار
> بثلاثة كيانات وصف لكلٍّ. لا أثر على الحذف، لكن العدد كان يكذب.
>
> **تحديث م71:** المواعيد لم تعد تُؤرشف — تُحذف. بقرار المالك الصريح:
> الموعد الماضي يُشطب تلقائياً بعد **7 أيام** (كنسة يومية بعد أول مزامنة،
> بتاريخ الموعد نفسه فلا يُشطب مستقبلي أبداً)، ثم يُحذف شاهده **نهائياً**
> من الخادم بعد **30 يوماً** (`purge_expired` أسبوعياً — الفارق يضمن وصول
> الحذف للأجهزة الغائبة). نظام الدور باقٍ على تنظيفه اليومي (م56) —
> أشدّ أصلاً. الحذف الفعلي يشمل شواهد كل الكيانات لا المواعيد وحدها.
> قاعدة أرشفة المواعيد أدناه (120 يوماً) صارت شبكة أمان خاملة عملياً:
> لا موعد يبلغ عمر الأرشفة حياً. التفاصيل: `lib/data/sync/retention.dart`
> والمهاجرة 0021 (أرضية خادمية 7 أيام على `purge_expired` كي لا يمحو
> عميل معطوب شواهد طازجة قبل تسليم رسالتها).

## للمالك (بلا كلام تقني)

**شو تعمل؟** البيانات القديمة (سجلات وأشعة أقدم من ٧ أشهر، ومواعيد ماضية
أقدم من ٤ أشهر) تُنقل تلقائياً مرة بالشهر من القاعدة الرئيسية (الغالية)
إلى مخزن الملفات (شبه المجاني). أجهزة العيادة ما تتأثر أبداً — كل جهاز
يحتفظ بكل شيء عنده.

**النتيجة؟** فاتورة القاعدة تتوقف عن النمو — تثبت للأبد مهما مرّت السنين.
بالأرقام: ٥٠٠ عيادة تبقى تحت ٢٥ دولار شهرياً إلى الأبد.

**الأمان؟** ما ينحذف شيء من القاعدة قبل ما يتأكد النظام أن النسخة وصلت
سليمة للمخزن (يقرأها ويطابق بصمتها). لو فشل أي شيء بالنص — ما ينحذف شيء،
ويعيد المحاولة بالشهر الجاي. والمرضى والديون **ما تُؤرشف أبداً** مهما قدُمت.

**وين ألاقيها؟** الإعدادات ← التخزين والنسخ الاحتياطي ← «الأرشفة الباردة».
شغّالة تلقائياً، وفيها زر «أرشفة الآن» وزر «استرجاع الأرشيف» للجهاز الجديد.

---

## للمبرمج

### البنية

```
ColdArchive (lib/data/sync/archive/cold_archive.dart)
  selectEligible()  انتقاء محلي بقواعد النوافذ
  run()             حزمة NDJSON.gz ← رفع R2 ← تحقق بصمة ← فهرس ← RPC حذف
  restore()         فهرس ← حزم ← mergeRemoteRow (ترطيب نظيف يحترم HLC)
ArchiveScheduler    يستمع لدورات المحرك المكتملة، مرة كل ٣٠ يوماً كحد أقصى
archive_rows RPC    (المهاجرة 0020) حذف بحُرّاس خادمية مستقلة
```

### القواعد

| الكيان | النافذة | ملاحظة |
|---|---|---|
| records / xrays | **90 يوماً** | م73 — كانت 210. القياس: 15.4 ← 8.1 م.ب للحساب. وصار التقليص آمناً بعد حارس المؤشر الخادمي (المهاجرة 0022) الذي يوقف الأرشفة عند أدنى مؤشر جهاز نشط. قابلة للضبط من الإعدادات (90/180/365) |
| appointments | 120 يوماً | **الماضية فقط** — تاريخ الحمولة يُفحص، فموعد مستقبلي لا يُؤرشف مهما قدُم إنشاؤه |
| patients / debts / settings | **أبداً** | هوية، مال معلّق، حالة حيّة |

- صف `_dirty` (تعديل غير مدفوع) لا يُؤرشف.
- صف بلا `_mod` لا يُؤرشف — قِدمه غير مثبَت.
- شواهد القبور القديمة تُؤرشف (تدخل الحزم وتُحذف من الخادم).
- المصغّرات لا تدخل الحزم أبداً — الاسترجاع يرطّبها من العامل (`?v=thumb`).

### الفشل المغلق — الترتيب مقصود

```
حزمة ← رفع ← إعادة قراءة + مطابقة SHA-256 ← فهرس (مع تحققه) ← حذف خادمي ← علامة مائية
```

أي فشل في أي خطوة ⇒ توقف كامل بلا حذف وبلا تقدّم للعلامة المائية، فالمحاولة
التالية تلتقط الصفوف نفسها. إعادة المحاولة آمنة دائماً (الحذف idempotent).

### ضمانات الخادم (مستقلة عن العميل — مهاجرة 0020)

- `user_id = auth.uid()` دائماً — لا لمس لصفوف الغير.
- قائمة سماح صريحة: records/appointments/xrays — **patients وdebts مرفوضة
  بنيوياً** حتى لو طلبها عميل معطوب.
- أرضية عمر **90 يوماً** تُفرض بـ`GREATEST` — `p_min_age_days=0` لا يخدع.
- سقف 10,000 عنصر بالنداء.
- مُختبَرة بسبعة فحوص على نسخة محلية من مخطط الإنتاج، ومطبَّقة على
  الإنتاج (خاملة حتى تصل نسخة تطبيق تستدعيها).

### ⚠ متطلب عامل Cloudflare الوحيد

الحزم تُرفع عبر عامل R2 القائم نفسه بمفاتيح `archive/{uid}/…` وبـ
`Content-Type: application/gzip`. **إن كان العامل يقيّد الرفع بامتدادات
الصور أو ببادئة `xrays/`، أضف سطر سماح لبادئة `archive/`.** إن رفض
العامل الرفع فالميزة تفشل مغلقةً (لا حذف) — لا خطر، فقط لا أرشفة حتى
يُسمح.

### الجهاز الجديد

يسحب النافذة الساخنة فقط من المزامنة. زر «استرجاع الأرشيف» في الإعدادات
يجلب الفهرس والحزم ويرطّب الناقص **نظيفاً** عبر `mergeRemoteRow` — يحترم
HLC فلا يكتب فوق أي شيء أحدث محلياً، ويطبّق شواهد القبور. حزمة فاسدة
تُتخطى وتُبلَّغ عدّاً ولا تعطّل البقية.

### الاختبارات

`test/m70_cold_archive_test.dart` — 10 اختبارات: الانتقاء (النوافذ،
المحظورات، المستقبلي، المتسخ، بلا `_mod`)، الفشل المغلق (رفع/بصمة/RPC)،
المحتوى (بلا مصغّرات/أعلام داخلية، فهرس صحيح)، العلامة المائية
(لا إعادة رفع، والصف المُحيا يُلتقط ثانية)، والاسترجاع (نظيف، لا يكتب
فوق الأحدث، يطبّق القبور، يتخطى الفاسد).
