# التدقيق النهائي العدائي — م84

تدقيقٌ أخير بعد اكتمال المراحل الستّ، مرجعُه أرشيف v84. المنهج: **ثلاث
مراجعات عدائية مستقلة متوازية** على الأنظمة الفرعية التي اختُبرت ولم
تُراجَع بالعمق نفسه (المرحلة السادسة كانت قد رُوجعت سابقاً)، وكلُّ ادّعاءٍ
يُثبَت **بالتشغيل** لا بالقراءة، مع تحكّمٍ سلبي حيث يلزم.

## الحصيلة

| # | الشدّة | النظام | العيب | الحالة |
|---|---|---|---|---|
| C1 | حرج | المزامنة | تباعُدٌ دائم على حقلٍ ماليّ: تراجُع جهازٍ آخر يُهمَل ويُدهَس على الخادم | ✅ أُصلح |
| C1 | حرج | الأرشفة | رأسٌ (HEAD) فاشلٌ عابر يُعامَل «غياباً» فيُدهَس فهرسٌ سليم وتُيتَّم الحزم | ✅ أُصلح |
| H1 | عالٍ | المال | الأرقام العربية‑الهندية تُحلَّل صفراً صامتاً ⇒ تكلفةٌ تُمحى، توزيعُ ربحٍ خاطئ | ✅ أُصلح |
| M1 | متوسط | المستودعات | `bulkUpsert` يُسقط تحويل أسماء الأعمدة فتنفصل الأعمدة المالية عن مصدرها | ✅ أُصلح |

**كل الأربعة مُثبَتة بالتشغيل ومحروسة باختبارات انحدار دائمة في
`test/m84_audit_fixes_test.dart` (‏٩ اختبارات)، ولكلٍّ منها تحكّمٌ سلبي
يُثبت أنه يفشل بلا الإصلاح.** المجموع: ٦٧٦ اختباراً خضراء (‏٦٦٧ ثم ٩).

---

## الإصلاحات الأربعة

### C1 — التباعد المالي (محرك المزامنة)

**العيب:** لقطةُ الأساس الظلّية (shadow) لا تتقدّم على «صدى دفعنا نحن»
(قاعدة v27 تتخطّاه عمداً). فتبقى عالقةً عند القيمة **قبل** تعديلنا. وحين
يتراجع جهازٌ آخر إلى تلك القيمة القديمة، يقرأ الدمجُ الثلاثي
`remote == base ⇒ تغيّر المحلي فقط ⇒ يفوز المحلي`، فيُهمَل التراجع
**ويُعاد دفع قيمتنا فوقه على الخادم** — تباعُدٌ دائم لكل الأجهزة.

**السيناريو المُثبَت:** ‏A=100 ثم B=250 (مدفوع) ثم A→100 ⇒ يبقى الخادم 250.

**الإصلاح** (`push.dart:markRowSynced`): تُثبَّت اللقطة عند **تأكيد الدفع**
— القيمة التي دفعناها وأقرّها الخادم هي الأساس المشترك الحقيقي. وهذا
المكان الصحيح دلالياً، ولا يُعيد عيب v27 (لقطةٌ بمفتاحٍ لا يملكه البعيد)
لأن القيمة هنا **مدفوعةٌ ومُقرّة** فالخادم يحملها فعلاً. مشروطٌ بـ
`stillSame` كي لا تُثبَّت قيمةٌ عُدّل الصف بعدها.

**التحقق من عدم إعادة v27:** ‏١١٨ اختبار دمج ومزامنة (‏m2, m48, m49, m53,
m67, m81, m70, m77, m80) خضراء بعد الإصلاح.

### C1 — فقدان دليل الأرشيف (الأرشفة الباردة)

**العيب:** `R2HeadResult(ok:false)` كان يخلط **الغياب المؤكَّد (404)**
بـ**إخفاق القراءة العابر** (‏5xx، مهلة، رمزٌ منتهٍ). فجهازٌ جديد
(‏`expected=0`) يقرأ فهرس الأرشيف أثناء عطلٍ لحظيّ يستنتج «الفهرس غائب ⇒
أول تشغيل»، فيبني فهرساً جديداً فيه الحزمة الجديدة وحدها **ويدهس** الفهرس
السليم على الخادم، فتُيتَّم كل الحزم السابقة بلا سبيلٍ لاستعادتها من داخل
المنتج.

**الإصلاح:** أُضيف `notFound` إلى `R2HeadResult` — يُضبط `true` عند 404/410
**فقط**. و`headObject` تُميّزه من أي إخفاقٍ آخر. و`_fetchIndexGuarded` لا
تُعلن «غياباً» إلا حين يؤكّده الخادم صراحةً؛ والوجودُ المجهول فشلٌ يُجهض
المسار الهدّام. والمزيّفات في الاختبارات صارت تُرجع `notFound` مطابقةً
لدلالة R2 الحقيقية (كائنٌ غائب ⇒ 404).

### H1 — الأرقام العربية في المال

**العيب:** كل الواجهة عربية، ولوحةُ مفاتيح رقمية في بيئةٍ عربية تُخرج
`٠–٩`. و`double.tryParse` لا يفهمها ⇒ `NaN` ⇒ `jsNumOr0` يعيد **صفراً
صامتاً**. فتكلفةُ مختبرٍ تُدخَل `١٥٠٠` تصير 0، فيُحسب صافي الربح ونصيبُ
الطبيب على مبلغٍ خاطئ بلا أي خطأٍ ظاهر.

**الإصلاح** (`js_compat.dart:jsNumber`): طيُّ الأرقام العربية‑الهندية
والفارسية إلى ASCII في **نقطة الاختناق الوحيدة** لكل تحويلٍ رقمي، مع
الفاصلة العشرية العربية `٫` وتجاهل فاصل الآلاف `٬`. المدخل ASCII لا
يتغيّر بتّةً.

### M1 — اتّساق `bulkUpsert`

**العيب:** `bulkUpsert` كان يُسقط `applyColumnAliases` الذي يطبّقه `upsert`
المفرد، فتهبط المفاتيح camelCase في مدونة `data` وحدها بينما تبقى الأعمدة
الحقيقية صفراً. لا مسار شحنٍ يُصيبه اليوم، لكنه «مسدّسٌ مذخور»: أول استيراد
جماعي من كائنات المجال يُصيب — وهو عينُ صنف عطل `isDebt` الذي أُصلح سابقاً.

**الإصلاح:** إضافة `applyColumnAliases` لتتّسق مع المفرد.

---

## بنودٌ وُثِّقت ولم تُصلَح — بقرارٍ لا إهمالاً

المراجعات رصدت بنوداً أدنى شدّةً. أُبقيت عمداً، وهذا مسوّغُ كلٍّ منها:

**كلها في سياق «مرحلة البناء، لا بيانات حقيقية، منصّتان أندرويد/ويندوز».**

- **شيفرةٌ ميتة** (`reconcile_guard.dart`, `hlc_order.dart`, بعض دوال
  `debts_repository`/`getMonthlyTotals`): لا مستهلك في الإنتاج. غير ضارّة،
  وحذفُها تنظيفٌ مؤجَّل لا إصلاح.
- **مهلاتٌ غائبة على مسارات خلفية** (‏`gotrue.refresh`, `headObject`):
  قد تُعلّق عملية أرشفةٍ خلفية حتى إعادة التشغيل. لا فقدان بيانات، وأثرها
  مقصورٌ على مؤقّتٍ لا يمسّ العمل التفاعلي. تحسينُ متانةٍ مؤجَّل.
- **إعادة رفعٍ للأرشيف بلا تراجع أُسّي عند الفشل المتكرّر**: كلفةُ نطاقٍ
  وتخزين لا فقدان. تُعالَج قبل النشر واسع النطاق.
- **مفتاحُ حزمةٍ بدقّة الثانية قد يتصادم** على إعادةٍ في الثانية نفسها:
  نادرٌ جداً، ويُعالَج بإضافة البصمة إلى المفتاح لاحقاً.
- **تنبيهاتٌ أسلوبية** (‏`use_null_aware_elements` ×٣): `info` لا يكسر
  CI، وموروثةٌ من v84 في ملفاتٍ لم تُمسّ.

**قرارُ سوبابيس الأربعة** (المفتاح، التسجيل، تأكيد البريد، HIBP) خارج نطاق
هذا التدقيق — مقبولةُ المخاطر بقرار المالك (انظر `SECURITY_RUNBOOK_AR.md`).

---

## ما لم يتغيّر — تأكيدٌ لا اكتشاف

المراجعات الثلاث أكّدت — بالتشغيل — سلامةَ:

- ساعة HLC (الرتابة، إصلاح الإقلاع، رفض الانحراف)، وتبادليّةَ الدمج الحقلي،
  ولا‑أثريّةَ الدفع/السحب تحت التكرار والخلط.
- ترتيبَ الأرشفة على المسار السعيد: رفعٌ ← تحقّق ببصمة ← فهرسة ← حذفٌ
  خادميّ ← علامة مائية. **لا حذف صفٍّ محليٍّ في الأرشفة إطلاقاً.**
- حارسَ التجاوز الأمني: تجزئةٌ فارغة/قصيرة/بلا ملحٍ تُرفض؛ PBKDF2 ثابتُ
  الزمن؛ نصيبُ الطبيب مُشتقٌّ من لقطةٍ مجمّدة لا يَنجرف؛ شواهدُ الحذف
  مستبعَدةٌ من كل تجميعٍ ماليّ.
- ادّعاءٌ سابق لمراجعٍ (‏`r2_client.dart:99` كتمُ فشل) — **أُعيد التحقق
  وسقط**: رمزُ الحالة مفحوصٌ قبله.

---

## م85 — النقود بالقروش الصحيحة (بطلب المالك)

**القرار:** المالك (غير المبرمج) اختار تنفيذ التحويل الآن ما دام التطبيق في
مرحلة البناء بلا بيانات حقيقية — أسلمُ وقتٍ لتغييرٍ يمسّ الحساب المالي.

**ما تغيّر — الحساب لا التخزين:**
- وحدة نقود مركزية جديدة (`lib/core/money.dart`): كل جمعٍ وطرحٍ ونسبةٍ يجري
  على **قروشٍ صحيحة** (int) لا تنجرف أبداً، ثم يُقسَم على 100 للعرض.
- طبقات الحساب الحسّاسة (`profits_logic`, `treasury_logic`) تجمع بالقروش
  الصحيحة (`sumMoney`) بدل `fold` على `double` — فتقرير آلاف الصفوف لا يراكم
  غبار التقريب.
- حدود الكتابة (`record_saver`, `debt_actions`) تضبط كل مبلغ على القرش
  (`quantize`) فلا يُخزَّن غبارٌ دون القرش أبداً.

**ما لم يتغيّر عمداً — والسبب حاسم:**
- **صيغة التخزين والإرسال تبقى ديناراً عشرياً كما هي.** تغييرُ وحدة الإرسال
  إلى قروش بلا حقل إصدار في البروتوكول يجعل نسخةً قديمة تقرأ 1050 على أنها
  1050 ديناراً — فسادُ مبلغٍ بمعامل 100 بين الأجهزة. فالحساب بالقروش،
  والإرسال بالدينار المضبوط على القرش.
- **`recompute` (الحصص المشتقّة عند الدمج) يبقى على `jsRound` (تقريب لوحدةٍ
  كاملة)** — مطابقةً حرفية لتقارير تطبيق الويب الأصلي؛ تغييرُه يكسر تطابق
  التقارير الموثَّق.
- **مجاميع العرض لكل مريض** تبقى كما هي (صفوفٌ قليلة، لا انجراف على نطاقها).

**التحقّق:** وحدة النقود لها اختبار **تحكّم** يُثبت بالتشغيل أن الجمع الساذج
بالكسور ينجرف فعلاً (`0.1+0.1+0.1 ≠ 0.3`) بينما جمع القروش دقيقٌ تماماً. و
**77 اختباراً مالياً قائماً** بقيت خضراء (شبكة تكافؤ السلوك)، وإجمالي
**686 اختباراً** أخضر بلا انحدار.

---

## م86 — القفل يصمد عبر إغلاق التطبيق (رصده المالك)

**الملاحظة (من المالك):** «القفل بعد ١٠ دقائق خمول — لكن بمجرّد إغلاق
التطبيق يستطيع أي شخص الدخول مجدداً.»

**العيب — مؤكَّد بالتشغيل:** قفلُ الخمول كان حالةَ ذاكرةٍ فقط
(`lockedProvider`). فعند الإقلاع البارد تُستعاد الجلسة المحفوظة بلا كلمة
مرور، و`justLoggedIn=false` فيمضي إلى الرئيسية مباشرةً، والحالة تُصفَّر إلى
`false` — أي أن **إغلاق التطبيق وإعادة فتحه يتخطّى القفل كلّه**. القفل كان
يحمي لوحاً متروكاً مفتوحاً، لا جهازاً أُغلق وأُعيد فتحه.

**الإصلاح** (`post_login_gate.dart`): كلُّ إقلاعٍ باردٍ على **جلسةٍ مستعادة**
(لا دخولٌ صريح) يبدأ **مقفلاً** متى وُجد مُتحقِّق قفلٍ محلي — يطلب كلمة
المرور نفسها (أو الخروج). وهو سلوك المنتجات الطبية القياسي، ويعمل بلا
شبكة (التحقّق محليّ PBKDF2). والدخولُ الصريح (`justLoggedIn=true`) لا يُقفَل
لأن المستخدم صادَق للتوّ.

**التحقّق:** اختبار `م86` يُحاكي الإغلاق وإعادة الفتح (بذرُ دخولٍ في حاوية
تُرمى ثم تركيب `DentalApp` من جديد)، ويُثبت أن الشاشة تبدأ مقفلة وأن كلمة
المرور الصحيحة ترفعها — مع **تحكّمٍ سلبي** يفشل بلا الإصلاح. وحُدِّث `م36`
ليعبُر القفل كمستخدمٍ حقيقي. الإجمالي **688 اختباراً** أخضر بلا انحدار.
