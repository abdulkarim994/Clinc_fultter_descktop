# أجزاء حمولة المشروع base64: تُنقل كما هي حرفياً — أي تحويل CRLF على
# ويندوز يكسر فكّ base64 (أثبته فشل أول تشغيل للبناء). -text يمنع أي
# تطبيع لنهايات الأسطر عند checkout على كل المنصات.
_bootstrap/** -text
