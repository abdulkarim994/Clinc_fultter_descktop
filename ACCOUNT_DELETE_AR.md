# حذف الحساب — تطبيق المهاجرة 0031 (م93)

> ✅ **الحالة: مطبَّقة حيّاً** على Dentaldk بتاريخ 2026‑08‑01
> (`20260801132255 · delete_my_account`) ومتحقَّق منها ثلاثياً (بنية
> الدالة، الصلاحيات، ونداء خارجي رُدّ بـ42501). زر «حذف الحساب نهائياً»
> في نسخة 1.0.93 فما بعدها **فعّال بالكامل**. ما يلي محفوظ مرجعاً
> لإعادة التطبيق على مشروع جديد.

> ميزة «حذف الحساب» في التطبيق تحذف بيانات المستخدم حذفاً حقيقياً من
> Supabase وR2. الجزء الخاص بـ**R2** وبيانات الجهاز يعمل من التطبيق
> مباشرة. أما حذف صفّ **`auth.users`** فيتطلب دالةً على الخادم — هذا
> الملف يشرح تفعيلها مرةً واحدة.

---

## لماذا خطوة على الخادم؟

مفتاح anon المشحون مع التطبيق **لا يملك — بالتصميم — حذفَ مستخدمٍ من
`auth.users`**. البديل الآمن (بلا وضع مفتاح إداري في التطبيق) هو دالة
`SECURITY DEFINER` تعمل بصلاحيات مالكها وتحذف **بيانات المستدعي حصراً**
(`auth.uid()`). هذا نفس نمط المشروع في `0020_archive_rows_rpc` و
`0030_profiles`.

## ما تحذفه الدالة
- صفوف `sync_rows` وملحقات المزامنة (`applied_ops`، `device_cursors`،
  `device_sync_state`) الخاصة بالمستخدم.
- سجلّات التشغيل الخاصة به (`archive_log`، `archive_horizon`، `audit_log`).
- `profiles`: يسقط تلقائياً بالتتالي مع `auth.users`.
- صف `auth.users` نفسه.

## خطوات التطبيق (مرة واحدة)

1. افتح لوحة Supabase → مشروعك → **SQL Editor**.
2. الصق كامل محتوى الملف:
   `supabase/migrations/0031_delete_my_account.sql`
3. **Run**. (آمنٌ لإعادة التشغيل — `CREATE OR REPLACE`.)
4. تحقّق:
   ```sql
   select proname, prosecdef,
          (select array_agg(c) from unnest(coalesce(proconfig,'{}')) c
            where c like 'search_path=%') as sp
     from pg_proc p join pg_namespace n on n.oid = p.pronamespace
    where n.nspname = 'public' and proname = 'delete_my_account';
   ```
   المتوقع: صفٌّ واحد، `prosecdef = true`، و`search_path` مثبَّت.
5. وثّق تاريخ التطبيق في ترويسة الملف (كما في 0014/0030).

## سلوك التطبيق قبل/بعد التطبيق

- **قبل**: نداء الحذف يرجع PGRST202؛ التطبيق **يُجهض بنظافة** برسالة
  «خدمة حذف الحساب لم تُفعَّل على الخادم بعد (المهاجرة 0031) — لم يُحذف
  أي شيء». لا R2 ولا مسحٌ محلي. لا حالة نصف محذوفة.
- **بعد**: الحذف كامل: الخادم أولاً، ثم R2، ثم الجهاز، ثم خروج.

## ملاحظة أمان
- `audit_log` «لا يقبل إلا الإضافة» لصاحب الحساب أثناء حياته (م79/0026).
  محوُه **عند محو الحساب كله** مقصود (حق المستخدم في محو بياناته)، وتقوم
  به الدالة بمالكها لا العميل.
- الدالة تُنفَّذ للأدوار المصادَقة فقط (`authenticated`)، ومسار بحثها
  مثبَّت (حماية CVE-2018-1058) — نفس ضمانات دوال المشروع الأخرى.
- R2: التطبيق يحذف الكائنات التي يعرف مفاتيحها من قاعدته المحلية بعد
  آخر مزامنة. لو رغبت بتطهيرٍ شاملٍ من طرف الخادم (كائنات لا يعرفها جهاز
  بعينه)، أضِف لاحقاً نقطةَ «حذف بادئة المالك» في عامل R2 — خارج نطاق
  هذه الدفعة.
